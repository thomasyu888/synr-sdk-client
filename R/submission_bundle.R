# Platform Repository Service
#
# Platform Repository Service - Sage Bionetworks Platform 
#
# The version of the OpenAPI document: develop-SNAPSHOT
# Contact: thomas.yu@sagebionetworks.org
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title SubmissionBundle
#' @description SubmissionBundle Class
#' @format An \code{R6Class} generator object
#' @field submission  \link{SubmissionModel} [optional]
#'
#' @field submissionStatus  \link{SubmissionStatusModel} [optional]
#'
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
SubmissionBundle <- R6::R6Class(
  'SubmissionBundle',
  public = list(
    `submission` = NULL,
    `submissionStatus` = NULL,
    initialize = function(`submission`=NULL, `submissionStatus`=NULL, ...){
      local.optional.var <- list(...)
      if (!is.null(`submission`)) {
        stopifnot(R6::is.R6(`submission`))
        self$`submission` <- `submission`
      }
      if (!is.null(`submissionStatus`)) {
        stopifnot(R6::is.R6(`submissionStatus`))
        self$`submissionStatus` <- `submissionStatus`
      }
    },
    toJSON = function() {
      SubmissionBundleObject <- list()
      if (!is.null(self$`submission`)) {
        SubmissionBundleObject[['submission']] <-
          self$`submission`$toJSON()
      }
      if (!is.null(self$`submissionStatus`)) {
        SubmissionBundleObject[['submissionStatus']] <-
          self$`submissionStatus`$toJSON()
      }

      SubmissionBundleObject
    },
    fromJSON = function(SubmissionBundleJson) {
      SubmissionBundleObject <- jsonlite::fromJSON(SubmissionBundleJson)
      if (!is.null(SubmissionBundleObject$`submission`)) {
        submissionObject <- SubmissionModel$new()
        submissionObject$fromJSON(jsonlite::toJSON(SubmissionBundleObject$submission, auto_unbox = TRUE, digits = NA))
        self$`submission` <- submissionObject
      }
      if (!is.null(SubmissionBundleObject$`submissionStatus`)) {
        submissionStatusObject <- SubmissionStatusModel$new()
        submissionStatusObject$fromJSON(jsonlite::toJSON(SubmissionBundleObject$submissionStatus, auto_unbox = TRUE, digits = NA))
        self$`submissionStatus` <- submissionStatusObject
      }
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`submission`)) {
        sprintf(
        '"submission":
        %s
        ',
        jsonlite::toJSON(self$`submission`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`submissionStatus`)) {
        sprintf(
        '"submissionStatus":
        %s
        ',
        jsonlite::toJSON(self$`submissionStatus`$toJSON(), auto_unbox=TRUE, digits = NA)
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(SubmissionBundleJson) {
      SubmissionBundleObject <- jsonlite::fromJSON(SubmissionBundleJson)
      self$`submission` <- SubmissionModel$new()$fromJSON(jsonlite::toJSON(SubmissionBundleObject$submission, auto_unbox = TRUE, digits = NA))
      self$`submissionStatus` <- SubmissionStatusModel$new()$fromJSON(jsonlite::toJSON(SubmissionBundleObject$submissionStatus, auto_unbox = TRUE, digits = NA))
      self
    }
  )
)
