#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title PrincipalServices operations
#' @description PrincipalServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteRepoV1Email } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } email character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1NotificationEmail } \emph{  }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelPrincipalNotificationEmail} \cr
#'
#'
#' \item status code : 200 | the email address to use for notifications
#'
#' \item return type : OrgSagebionetworksRepoModelPrincipalNotificationEmail
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1Account2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_principal_account_setup_info \link{OrgSagebionetworksRepoModelPrincipalAccountSetupInfo}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAuthLoginResponse} \cr
#'
#'
#' \item status code : 201 | an access token, allowing the client to begin making authenticated requests
#'
#' \item return type : OrgSagebionetworksRepoModelAuthLoginResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1AccountEmailValidation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } portal_endpoint character
#' \item \emph{ @param } org_sagebionetworks_repo_model_auth_new_user \link{OrgSagebionetworksRepoModelAuthNewUser}
#'
#'
#' \item status code : 201 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1AccountIdEmailValidation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } portal_endpoint character
#' \item \emph{ @param } org_sagebionetworks_repo_model_auth_username \link{OrgSagebionetworksRepoModelAuthUsername}
#'
#'
#' \item status code : 201 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1Email } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_principal_email_validation_signed_token \link{OrgSagebionetworksRepoModelPrincipalEmailValidationSignedToken}
#' \item \emph{ @param } set_as_notification_email character
#'
#'
#' \item status code : 201 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1PrincipalAlias } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_principal_principal_alias_request \link{OrgSagebionetworksRepoModelPrincipalPrincipalAliasRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelPrincipalPrincipalAliasResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelPrincipalPrincipalAliasResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1PrincipalAvailable } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_principal_alias_check_request \link{OrgSagebionetworksRepoModelPrincipalAliasCheckRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelPrincipalAliasCheckResponse} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelPrincipalAliasCheckResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1NotificationEmail } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_auth_username \link{OrgSagebionetworksRepoModelAuthUsername}
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteRepoV1Email  ####################
#'
#' library(synclient)
#' var_email <- "email_example" # character | the email address to remove
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeleteRepoV1Email(var_email)
#'
#'
#' ####################  GetRepoV1NotificationEmail  ####################
#'
#' library(synclient)
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1NotificationEmail(data_file = "result.txt")
#' result <- api_instance$GetRepoV1NotificationEmail()
#' dput(result)
#'
#'
#' ####################  PostRepoV1Account2  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_principal_account_setup_info <- org.sagebionetworks.repo.model.principal.AccountSetupInfo$new("firstName_example", "lastName_example", "username_example", "password_example", org.sagebionetworks.repo.model.principal.EmailValidationSignedToken$new("concreteType_example", "hmac_example", 123, "expiresOn_example", "createdOn_example", "email_example", "userId_example")) # OrgSagebionetworksRepoModelPrincipalAccountSetupInfo | 
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1Account2(var_org_sagebionetworks_repo_model_principal_account_setup_infodata_file = "result.txt")
#' result <- api_instance$PostRepoV1Account2(var_org_sagebionetworks_repo_model_principal_account_setup_info)
#' dput(result)
#'
#'
#' ####################  PostRepoV1AccountEmailValidation  ####################
#'
#' library(synclient)
#' var_portal_endpoint <- "portal_endpoint_example" # character | the beginning of the URL included in the email verification message. When concatenated with         a list of ampersand (,&,) separated request parameters, must become a well formed URL. The concatenated         string must be included with the ,<a href=\"${POST.account2}\">,POST /account,</a>, request.
#' var_org_sagebionetworks_repo_model_auth_new_user <- org.sagebionetworks.repo.model.auth.NewUser$new("email_example", "firstName_example", "lastName_example", "userName_example", "oauthProvider_example", "subject_example", "encodedMembershipInvtnSignedToken_example") # OrgSagebionetworksRepoModelAuthNewUser | 
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' api_instance$PostRepoV1AccountEmailValidation(var_portal_endpoint, var_org_sagebionetworks_repo_model_auth_new_user)
#'
#'
#' ####################  PostRepoV1AccountIdEmailValidation  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | the ID of the user account to which the email address is to be added. Must match the ID of the user         making the request.
#' var_portal_endpoint <- "portal_endpoint_example" # character | the beginning of the URL included in the email verification message. When concatenated with         a list of ampersand (,&,) separated request parameters, must become a well formed URL. The concatenated         string must be included with the ,<a href=\"${POST.email}\">,POST /email,</a>, request.
#' var_org_sagebionetworks_repo_model_auth_username <- org.sagebionetworks.repo.model.auth.Username$new("email_example") # OrgSagebionetworksRepoModelAuthUsername | 
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PostRepoV1AccountIdEmailValidation(var_id, var_portal_endpoint, var_org_sagebionetworks_repo_model_auth_username)
#'
#'
#' ####################  PostRepoV1Email  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_principal_email_validation_signed_token <- org.sagebionetworks.repo.model.principal.EmailValidationSignedToken$new("concreteType_example", "hmac_example", 123, "expiresOn_example", "createdOn_example", "email_example", "userId_example") # OrgSagebionetworksRepoModelPrincipalEmailValidationSignedToken | 
#' var_set_as_notification_email <- "set_as_notification_email_example" # character | if true then the newly added email address becomes the address  used by the system for sending messages to the user. (Optional)
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PostRepoV1Email(var_org_sagebionetworks_repo_model_principal_email_validation_signed_token, set_as_notification_email = var_set_as_notification_email)
#'
#'
#' ####################  PostRepoV1PrincipalAlias  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_principal_principal_alias_request <- org.sagebionetworks.repo.model.principal.PrincipalAliasRequest$new("alias_example", "type_example") # OrgSagebionetworksRepoModelPrincipalPrincipalAliasRequest | 
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1PrincipalAlias(var_org_sagebionetworks_repo_model_principal_principal_alias_requestdata_file = "result.txt")
#' result <- api_instance$PostRepoV1PrincipalAlias(var_org_sagebionetworks_repo_model_principal_principal_alias_request)
#' dput(result)
#'
#'
#' ####################  PostRepoV1PrincipalAvailable  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_principal_alias_check_request <- org.sagebionetworks.repo.model.principal.AliasCheckRequest$new("alias_example", "type_example") # OrgSagebionetworksRepoModelPrincipalAliasCheckRequest | 
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1PrincipalAvailable(var_org_sagebionetworks_repo_model_principal_alias_check_requestdata_file = "result.txt")
#' result <- api_instance$PostRepoV1PrincipalAvailable(var_org_sagebionetworks_repo_model_principal_alias_check_request)
#' dput(result)
#'
#'
#' ####################  PutRepoV1NotificationEmail  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_auth_username <- org.sagebionetworks.repo.model.auth.Username$new("email_example") # OrgSagebionetworksRepoModelAuthUsername | 
#'
#' api_instance <- PrincipalServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PutRepoV1NotificationEmail(var_org_sagebionetworks_repo_model_auth_username)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
PrincipalServicesApi <- R6::R6Class(
  "PrincipalServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new PrincipalServicesApi.
    #'
    #' @description
    #' Initialize a new PrincipalServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param email the email address to remove
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteRepoV1Email = function(email, ...) {
      local_var_response <- self$DeleteRepoV1EmailWithHttpInfo(email, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param email the email address to remove
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteRepoV1EmailWithHttpInfo = function(email, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`email`)) {
        stop("Missing required parameter `email`.")
      }


      query_params[["email"]] <- `email`

      local_var_url_path <- "/repo/v1/email"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelPrincipalNotificationEmail
    #' @export
    GetRepoV1NotificationEmail = function(data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1NotificationEmailWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelPrincipalNotificationEmail) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1NotificationEmailWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/repo/v1/notificationEmail"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelPrincipalNotificationEmail", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_account_setup_info 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAuthLoginResponse
    #' @export
    PostRepoV1Account2 = function(org_sagebionetworks_repo_model_principal_account_setup_info, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1Account2WithHttpInfo(org_sagebionetworks_repo_model_principal_account_setup_info, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_account_setup_info 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAuthLoginResponse) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1Account2WithHttpInfo = function(org_sagebionetworks_repo_model_principal_account_setup_info, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_principal_account_setup_info`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_principal_account_setup_info`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_principal_account_setup_info`)) {
        local_var_body <- `org_sagebionetworks_repo_model_principal_account_setup_info`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/account2"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAuthLoginResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param portal_endpoint the beginning of the URL included in the email verification message. When concatenated with         a list of ampersand (,&,) separated request parameters, must become a well formed URL. The concatenated         string must be included with the ,<a href=\"${POST.account2}\">,POST /account,</a>, request.
    #' @param org_sagebionetworks_repo_model_auth_new_user 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostRepoV1AccountEmailValidation = function(portal_endpoint, org_sagebionetworks_repo_model_auth_new_user, ...) {
      local_var_response <- self$PostRepoV1AccountEmailValidationWithHttpInfo(portal_endpoint, org_sagebionetworks_repo_model_auth_new_user, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param portal_endpoint the beginning of the URL included in the email verification message. When concatenated with         a list of ampersand (,&,) separated request parameters, must become a well formed URL. The concatenated         string must be included with the ,<a href=\"${POST.account2}\">,POST /account,</a>, request.
    #' @param org_sagebionetworks_repo_model_auth_new_user 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1AccountEmailValidationWithHttpInfo = function(portal_endpoint, org_sagebionetworks_repo_model_auth_new_user, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`portal_endpoint`)) {
        stop("Missing required parameter `portal_endpoint`.")
      }

      if (missing(`org_sagebionetworks_repo_model_auth_new_user`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_auth_new_user`.")
      }



      query_params[["portalEndpoint"]] <- `portal_endpoint`

      if (!is.null(`org_sagebionetworks_repo_model_auth_new_user`)) {
        local_var_body <- `org_sagebionetworks_repo_model_auth_new_user`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/account/emailValidation"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the user account to which the email address is to be added. Must match the ID of the user         making the request.
    #' @param portal_endpoint the beginning of the URL included in the email verification message. When concatenated with         a list of ampersand (,&,) separated request parameters, must become a well formed URL. The concatenated         string must be included with the ,<a href=\"${POST.email}\">,POST /email,</a>, request.
    #' @param org_sagebionetworks_repo_model_auth_username 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostRepoV1AccountIdEmailValidation = function(id, portal_endpoint, org_sagebionetworks_repo_model_auth_username, ...) {
      local_var_response <- self$PostRepoV1AccountIdEmailValidationWithHttpInfo(id, portal_endpoint, org_sagebionetworks_repo_model_auth_username, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the user account to which the email address is to be added. Must match the ID of the user         making the request.
    #' @param portal_endpoint the beginning of the URL included in the email verification message. When concatenated with         a list of ampersand (,&,) separated request parameters, must become a well formed URL. The concatenated         string must be included with the ,<a href=\"${POST.email}\">,POST /email,</a>, request.
    #' @param org_sagebionetworks_repo_model_auth_username 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1AccountIdEmailValidationWithHttpInfo = function(id, portal_endpoint, org_sagebionetworks_repo_model_auth_username, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`portal_endpoint`)) {
        stop("Missing required parameter `portal_endpoint`.")
      }

      if (missing(`org_sagebionetworks_repo_model_auth_username`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_auth_username`.")
      }




      query_params[["portalEndpoint"]] <- `portal_endpoint`

      if (!is.null(`org_sagebionetworks_repo_model_auth_username`)) {
        local_var_body <- `org_sagebionetworks_repo_model_auth_username`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/account/{id}/emailValidation"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_email_validation_signed_token 
    #' @param set_as_notification_email (optional) if true then the newly added email address becomes the address  used by the system for sending messages to the user.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostRepoV1Email = function(org_sagebionetworks_repo_model_principal_email_validation_signed_token, set_as_notification_email = NULL, ...) {
      local_var_response <- self$PostRepoV1EmailWithHttpInfo(org_sagebionetworks_repo_model_principal_email_validation_signed_token, set_as_notification_email, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_email_validation_signed_token 
    #' @param set_as_notification_email (optional) if true then the newly added email address becomes the address  used by the system for sending messages to the user.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1EmailWithHttpInfo = function(org_sagebionetworks_repo_model_principal_email_validation_signed_token, set_as_notification_email = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_principal_email_validation_signed_token`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_principal_email_validation_signed_token`.")
      }



      query_params[["setAsNotificationEmail"]] <- `set_as_notification_email`

      if (!is.null(`org_sagebionetworks_repo_model_principal_email_validation_signed_token`)) {
        local_var_body <- `org_sagebionetworks_repo_model_principal_email_validation_signed_token`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/email"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_principal_alias_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelPrincipalPrincipalAliasResponse
    #' @export
    PostRepoV1PrincipalAlias = function(org_sagebionetworks_repo_model_principal_principal_alias_request, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1PrincipalAliasWithHttpInfo(org_sagebionetworks_repo_model_principal_principal_alias_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_principal_alias_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelPrincipalPrincipalAliasResponse) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1PrincipalAliasWithHttpInfo = function(org_sagebionetworks_repo_model_principal_principal_alias_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_principal_principal_alias_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_principal_principal_alias_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_principal_principal_alias_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_principal_principal_alias_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/principal/alias"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelPrincipalPrincipalAliasResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_alias_check_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelPrincipalAliasCheckResponse
    #' @export
    PostRepoV1PrincipalAvailable = function(org_sagebionetworks_repo_model_principal_alias_check_request, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1PrincipalAvailableWithHttpInfo(org_sagebionetworks_repo_model_principal_alias_check_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_principal_alias_check_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelPrincipalAliasCheckResponse) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1PrincipalAvailableWithHttpInfo = function(org_sagebionetworks_repo_model_principal_alias_check_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_principal_alias_check_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_principal_alias_check_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_principal_alias_check_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_principal_alias_check_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/principal/available"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelPrincipalAliasCheckResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_auth_username 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PutRepoV1NotificationEmail = function(org_sagebionetworks_repo_model_auth_username, ...) {
      local_var_response <- self$PutRepoV1NotificationEmailWithHttpInfo(org_sagebionetworks_repo_model_auth_username, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_auth_username 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1NotificationEmailWithHttpInfo = function(org_sagebionetworks_repo_model_auth_username, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_auth_username`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_auth_username`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_auth_username`)) {
        local_var_body <- `org_sagebionetworks_repo_model_auth_username`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/notificationEmail"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
