#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title VerificationServices operations
#' @description VerificationServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteRepoV1VerificationSubmissionId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id numeric
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1VerificationSubmission } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } verified_user_id numeric
#' \item \emph{ @param } current_verification_state \link{OrgSagebionetworksRepoModelVerificationVerificationStateEnum}
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } offset numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelVerificationVerificationPagedResults} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelVerificationVerificationPagedResults
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1VerificationSubmission } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_verification_verification_submission \link{OrgSagebionetworksRepoModelVerificationVerificationSubmission}
#' \item \emph{ @param } notification_unsubscribe_endpoint character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelVerificationVerificationSubmission} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelVerificationVerificationSubmission
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1VerificationSubmissionIdState } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id numeric
#' \item \emph{ @param } org_sagebionetworks_repo_model_verification_verification_state \link{OrgSagebionetworksRepoModelVerificationVerificationState}
#' \item \emph{ @param } notification_unsubscribe_endpoint character
#'
#'
#' \item status code : 201 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteRepoV1VerificationSubmissionId  ####################
#'
#' library(synclient)
#' var_id <- 3.4 # numeric | 
#'
#' api_instance <- VerificationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeleteRepoV1VerificationSubmissionId(var_id)
#'
#'
#' ####################  GetRepoV1VerificationSubmission  ####################
#'
#' library(synclient)
#' var_verified_user_id <- 3.4 # numeric | filter on the user requesting verification (optional) (Optional)
#' var_current_verification_state <- org.sagebionetworks.repo.model.verification.VerificationStateEnum$new() # OrgSagebionetworksRepoModelVerificationVerificationStateEnum | filter on the state of the verification submission (optional) (Optional)
#' var_limit <- 3.4 # numeric | page size pagination parameter (optional) (Optional)
#' var_offset <- 3.4 # numeric | page start pagination parameter (zero offset, optional) (Optional)
#'
#' api_instance <- VerificationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1VerificationSubmission(verified_user_id = var_verified_user_id, current_verification_state = var_current_verification_state, limit = var_limit, offset = var_offsetdata_file = "result.txt")
#' result <- api_instance$GetRepoV1VerificationSubmission(verified_user_id = var_verified_user_id, current_verification_state = var_current_verification_state, limit = var_limit, offset = var_offset)
#' dput(result)
#'
#'
#' ####################  PostRepoV1VerificationSubmission  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_verification_verification_submission <- org.sagebionetworks.repo.model.verification.VerificationSubmission$new("id_example", "createdOn_example", "createdBy_example", "firstName_example", "lastName_example", c("emails_example"), "notificationEmail_example", "location_example", "company_example", "orcid_example", c(org.sagebionetworks.repo.model.verification.VerificationState$new("createdOn_example", "createdBy_example", "state_example", "reason_example", "notes_example")), c(org.sagebionetworks.repo.model.verification.AttachmentMetadata$new("fileName_example", "id_example"))) # OrgSagebionetworksRepoModelVerificationVerificationSubmission | 
#' var_notification_unsubscribe_endpoint <- "notification_unsubscribe_endpoint_example" # character |  (Optional)
#'
#' api_instance <- VerificationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1VerificationSubmission(var_org_sagebionetworks_repo_model_verification_verification_submission, notification_unsubscribe_endpoint = var_notification_unsubscribe_endpointdata_file = "result.txt")
#' result <- api_instance$PostRepoV1VerificationSubmission(var_org_sagebionetworks_repo_model_verification_verification_submission, notification_unsubscribe_endpoint = var_notification_unsubscribe_endpoint)
#' dput(result)
#'
#'
#' ####################  PostRepoV1VerificationSubmissionIdState  ####################
#'
#' library(synclient)
#' var_id <- 3.4 # numeric | the ID of the verification submission
#' var_org_sagebionetworks_repo_model_verification_verification_state <- org.sagebionetworks.repo.model.verification.VerificationState$new("createdOn_example", "createdBy_example", "state_example", "reason_example", "notes_example") # OrgSagebionetworksRepoModelVerificationVerificationState | 
#' var_notification_unsubscribe_endpoint <- "notification_unsubscribe_endpoint_example" # character | the portal prefix for one-click email unsubscription (Optional)
#'
#' api_instance <- VerificationServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PostRepoV1VerificationSubmissionIdState(var_id, var_org_sagebionetworks_repo_model_verification_verification_state, notification_unsubscribe_endpoint = var_notification_unsubscribe_endpoint)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
VerificationServicesApi <- R6::R6Class(
  "VerificationServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new VerificationServicesApi.
    #'
    #' @description
    #' Initialize a new VerificationServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteRepoV1VerificationSubmissionId = function(id, ...) {
      local_var_response <- self$DeleteRepoV1VerificationSubmissionIdWithHttpInfo(id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteRepoV1VerificationSubmissionIdWithHttpInfo = function(id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/repo/v1/verificationSubmission/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_user_id (optional) filter on the user requesting verification (optional)
    #' @param current_verification_state (optional) filter on the state of the verification submission (optional)
    #' @param limit (optional) page size pagination parameter (optional)
    #' @param offset (optional) page start pagination parameter (zero offset, optional)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelVerificationVerificationPagedResults
    #' @export
    GetRepoV1VerificationSubmission = function(verified_user_id = NULL, current_verification_state = NULL, limit = NULL, offset = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1VerificationSubmissionWithHttpInfo(verified_user_id, current_verification_state, limit, offset, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_user_id (optional) filter on the user requesting verification (optional)
    #' @param current_verification_state (optional) filter on the state of the verification submission (optional)
    #' @param limit (optional) page size pagination parameter (optional)
    #' @param offset (optional) page start pagination parameter (zero offset, optional)
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelVerificationVerificationPagedResults) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1VerificationSubmissionWithHttpInfo = function(verified_user_id = NULL, current_verification_state = NULL, limit = NULL, offset = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      query_params[["verifiedUserId"]] <- `verified_user_id`

      query_params[["currentVerificationState"]] <- `current_verification_state`

      query_params[["limit"]] <- `limit`

      query_params[["offset"]] <- `offset`

      local_var_url_path <- "/repo/v1/verificationSubmission"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelVerificationVerificationPagedResults", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_verification_verification_submission 
    #' @param notification_unsubscribe_endpoint (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelVerificationVerificationSubmission
    #' @export
    PostRepoV1VerificationSubmission = function(org_sagebionetworks_repo_model_verification_verification_submission, notification_unsubscribe_endpoint = NULL, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1VerificationSubmissionWithHttpInfo(org_sagebionetworks_repo_model_verification_verification_submission, notification_unsubscribe_endpoint, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_verification_verification_submission 
    #' @param notification_unsubscribe_endpoint (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelVerificationVerificationSubmission) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1VerificationSubmissionWithHttpInfo = function(org_sagebionetworks_repo_model_verification_verification_submission, notification_unsubscribe_endpoint = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_verification_verification_submission`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_verification_verification_submission`.")
      }



      query_params[["notificationUnsubscribeEndpoint"]] <- `notification_unsubscribe_endpoint`

      if (!is.null(`org_sagebionetworks_repo_model_verification_verification_submission`)) {
        local_var_body <- `org_sagebionetworks_repo_model_verification_verification_submission`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/verificationSubmission"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelVerificationVerificationSubmission", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the verification submission
    #' @param org_sagebionetworks_repo_model_verification_verification_state 
    #' @param notification_unsubscribe_endpoint (optional) the portal prefix for one-click email unsubscription
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostRepoV1VerificationSubmissionIdState = function(id, org_sagebionetworks_repo_model_verification_verification_state, notification_unsubscribe_endpoint = NULL, ...) {
      local_var_response <- self$PostRepoV1VerificationSubmissionIdStateWithHttpInfo(id, org_sagebionetworks_repo_model_verification_verification_state, notification_unsubscribe_endpoint, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the verification submission
    #' @param org_sagebionetworks_repo_model_verification_verification_state 
    #' @param notification_unsubscribe_endpoint (optional) the portal prefix for one-click email unsubscription
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1VerificationSubmissionIdStateWithHttpInfo = function(id, org_sagebionetworks_repo_model_verification_verification_state, notification_unsubscribe_endpoint = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_verification_verification_state`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_verification_verification_state`.")
      }




      query_params[["notificationUnsubscribeEndpoint"]] <- `notification_unsubscribe_endpoint`

      if (!is.null(`org_sagebionetworks_repo_model_verification_verification_state`)) {
        local_var_body <- `org_sagebionetworks_repo_model_verification_verification_state`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/verificationSubmission/{id}/state"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
