#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title DrsServices operations
#' @description DrsServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetGa4ghDrsV1ObjectsObjectId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } object_id character
#' \item \emph{ @param } expand character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDrsDrsObject} \cr
#'
#'
#' \item status code : 200 | the DRS object
#'
#' \item return type : OrgSagebionetworksRepoModelDrsDrsObject
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetGa4ghDrsV1ObjectsObjectIdAccessAccessId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } object_id character
#' \item \emph{ @param } access_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDrsAccessUrl} \cr
#'
#'
#' \item status code : 200 | the presigned url to download a file
#'
#' \item return type : OrgSagebionetworksRepoModelDrsAccessUrl
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetGa4ghDrsV1ServiceInfo } \emph{  }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDrsServiceInformation} \cr
#'
#'
#' \item status code : 200 | the drs service information
#'
#' \item return type : OrgSagebionetworksRepoModelDrsServiceInformation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetGa4ghDrsV1ObjectsObjectId  ####################
#'
#' library(synclient)
#' var_object_id <- "object_id_example" # character | 
#' var_expand <- "expand_example" # character | 
#'
#' api_instance <- DrsServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGa4ghDrsV1ObjectsObjectId(var_object_id, var_expanddata_file = "result.txt")
#' result <- api_instance$GetGa4ghDrsV1ObjectsObjectId(var_object_id, var_expand)
#' dput(result)
#'
#'
#' ####################  GetGa4ghDrsV1ObjectsObjectIdAccessAccessId  ####################
#'
#' library(synclient)
#' var_object_id <- "object_id_example" # character | 
#' var_access_id <- "access_id_example" # character | 
#'
#' api_instance <- DrsServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGa4ghDrsV1ObjectsObjectIdAccessAccessId(var_object_id, var_access_iddata_file = "result.txt")
#' result <- api_instance$GetGa4ghDrsV1ObjectsObjectIdAccessAccessId(var_object_id, var_access_id)
#' dput(result)
#'
#'
#' ####################  GetGa4ghDrsV1ServiceInfo  ####################
#'
#' library(synclient)
#'
#' api_instance <- DrsServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetGa4ghDrsV1ServiceInfo(data_file = "result.txt")
#' result <- api_instance$GetGa4ghDrsV1ServiceInfo()
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DrsServicesApi <- R6::R6Class(
  "DrsServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new DrsServicesApi.
    #'
    #' @description
    #' Initialize a new DrsServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param object_id 
    #' @param expand 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDrsDrsObject
    #' @export
    GetGa4ghDrsV1ObjectsObjectId = function(object_id, expand, data_file = NULL, ...) {
      local_var_response <- self$GetGa4ghDrsV1ObjectsObjectIdWithHttpInfo(object_id, expand, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param object_id 
    #' @param expand 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDrsDrsObject) with additional information such as HTTP status code, headers
    #' @export
    GetGa4ghDrsV1ObjectsObjectIdWithHttpInfo = function(object_id, expand, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_id`)) {
        stop("Missing required parameter `object_id`.")
      }

      if (missing(`expand`)) {
        stop("Missing required parameter `expand`.")
      }



      query_params[["expand"]] <- `expand`

      local_var_url_path <- "/ga4gh/drs/v1/objects/{object_id}"
      if (!missing(`object_id`)) {
        local_var_url_path <- gsub("\\{object_id\\}", URLencode(as.character(`object_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDrsDrsObject", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param object_id 
    #' @param access_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDrsAccessUrl
    #' @export
    GetGa4ghDrsV1ObjectsObjectIdAccessAccessId = function(object_id, access_id, data_file = NULL, ...) {
      local_var_response <- self$GetGa4ghDrsV1ObjectsObjectIdAccessAccessIdWithHttpInfo(object_id, access_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param object_id 
    #' @param access_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDrsAccessUrl) with additional information such as HTTP status code, headers
    #' @export
    GetGa4ghDrsV1ObjectsObjectIdAccessAccessIdWithHttpInfo = function(object_id, access_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`object_id`)) {
        stop("Missing required parameter `object_id`.")
      }

      if (missing(`access_id`)) {
        stop("Missing required parameter `access_id`.")
      }



      local_var_url_path <- "/ga4gh/drs/v1/objects/{object_id}/access/{access_id}"
      if (!missing(`object_id`)) {
        local_var_url_path <- gsub("\\{object_id\\}", URLencode(as.character(`object_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`access_id`)) {
        local_var_url_path <- gsub("\\{access_id\\}", URLencode(as.character(`access_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDrsAccessUrl", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDrsServiceInformation
    #' @export
    GetGa4ghDrsV1ServiceInfo = function(data_file = NULL, ...) {
      local_var_response <- self$GetGa4ghDrsV1ServiceInfoWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDrsServiceInformation) with additional information such as HTTP status code, headers
    #' @export
    GetGa4ghDrsV1ServiceInfoWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/ga4gh/drs/v1/service-info"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDrsServiceInformation", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
