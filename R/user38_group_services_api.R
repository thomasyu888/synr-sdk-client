#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title User38GroupServices operations
#' @description User38GroupServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetRepoV1UserGroup } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } offset integer
#' \item \emph{ @param } limit integer
#' \item \emph{ @param } sort character
#' \item \emph{ @param } ascending character
#' \item \emph{ @returnType } \link{PaginatedResultsOfUserGroup} \cr
#'
#'
#' \item status code : 200 | The UserGroups for individuals
#'
#' \item return type : PaginatedResultsOfUserGroup
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetRepoV1UserGroup  ####################
#'
#' library(synclient)
#' var_offset <- 56 # integer |  (Optional)
#' var_limit <- 56 # integer |  (Optional)
#' var_sort <- "sort_example" # character |  (Optional)
#' var_ascending <- "ascending_example" # character |  (Optional)
#'
#' api_instance <- User38GroupServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1UserGroup(offset = var_offset, limit = var_limit, sort = var_sort, ascending = var_ascendingdata_file = "result.txt")
#' result <- api_instance$GetRepoV1UserGroup(offset = var_offset, limit = var_limit, sort = var_sort, ascending = var_ascending)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
User38GroupServicesApi <- R6::R6Class(
  "User38GroupServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new User38GroupServicesApi.
    #'
    #' @description
    #' Initialize a new User38GroupServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset (optional) No description
    #' @param limit (optional) No description
    #' @param sort (optional) No description
    #' @param ascending (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfUserGroup
    #' @export
    GetRepoV1UserGroup = function(offset = NULL, limit = NULL, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1UserGroupWithHttpInfo(offset, limit, sort, ascending, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset (optional) No description
    #' @param limit (optional) No description
    #' @param sort (optional) No description
    #' @param ascending (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfUserGroup) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1UserGroupWithHttpInfo = function(offset = NULL, limit = NULL, sort = NULL, ascending = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      query_params[["sort"]] <- `sort`

      query_params[["ascending"]] <- `ascending`

      local_var_url_path <- "/repo/v1/userGroup"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfUserGroup", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
