#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title OpenIDConnectServices operations
#' @description OpenIDConnectServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteAuthV1Oauth2ClientId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2AuditGrantedClients } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } next_page_token character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClientAuthorizationHistoryList} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClientAuthorizationHistoryList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2AuditGrantedClientsClientIdTokens } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } client_id character
#' \item \emph{ @param } next_page_token character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformationList} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformationList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2AuditTokensTokenIdMetadata } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } token_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2Client } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } next_page_token character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClientList} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClientList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2ClientId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClient} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClient
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2Jwks } \emph{  }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthJsonWebKeySet} \cr
#'
#'
#' \item status code : 200 | the JSON Web Key Set
#'
#' \item return type : OrgSagebionetworksRepoModelOauthJsonWebKeySet
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2TokenTokenIdMetadata } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } verified_oauth_client_id character
#' \item \emph{ @param } token_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1Oauth2Userinfo } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } synapse_authorization character
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : object
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetAuthV1WellKnownOpenidConfiguration } \emph{  }
#'
#' \itemize{
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOIDConnectConfiguration} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOIDConnectConfiguration
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2AuditGrantedClientsClientIdRevoke } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } client_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2AuditTokensTokenIdRevoke } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } token_id character
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Client } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_o_auth_client \link{OrgSagebionetworksRepoModelOauthOAuthClient}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClient} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClient
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2ClientSecretId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClientIdAndSecret} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClientIdAndSecret
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Consent } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_oidc_authorization_request \link{OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthAuthorizationResponse} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthAuthorizationResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Consentcheck } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_oidc_authorization_request \link{OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthConsentGrantedResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthConsentGrantedResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Description } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_oidc_authorization_request \link{OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequestDescription} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequestDescription
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Revoke } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } verified_oauth_client_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request \link{OrgSagebionetworksRepoModelOauthOAuthTokenRevocationRequest}
#'
#'
#' \item status code : 204 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Token } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } verified_oauth_client_id character
#' \item \emph{ @param } grant_type \link{OrgSagebionetworksRepoModelOauthOAuthGrantType}
#' \item \emph{ @param } code character
#' \item \emph{ @param } redirect_uri character
#' \item \emph{ @param } refresh_token character
#' \item \emph{ @param } scope character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOIDCTokenResponse} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOIDCTokenResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostAuthV1Oauth2Userinfo } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } synapse_authorization character
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : object
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutAuthV1Oauth2AuditTokensTokenIdMetadata } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } token_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information \link{OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutAuthV1Oauth2ClientId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_o_auth_client \link{OrgSagebionetworksRepoModelOauthOAuthClient}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClient} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClient
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutAuthV1Oauth2ClientIdVerificationPrecheck } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_oauth_o_auth_client \link{OrgSagebionetworksRepoModelOauthOAuthClient}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelOauthOAuthClientVerificationPrecheckResult} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelOauthOAuthClientVerificationPrecheckResult
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteAuthV1Oauth2ClientId  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | the ID of the client to delete
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeleteAuthV1Oauth2ClientId(var_id)
#'
#'
#' ####################  GetAuthV1Oauth2AuditGrantedClients  ####################
#'
#' library(synclient)
#' var_next_page_token <- "next_page_token_example" # character |  (Optional)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2AuditGrantedClients(next_page_token = var_next_page_tokendata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2AuditGrantedClients(next_page_token = var_next_page_token)
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2AuditGrantedClientsClientIdTokens  ####################
#'
#' library(synclient)
#' var_client_id <- "client_id_example" # character | 
#' var_next_page_token <- "next_page_token_example" # character |  (Optional)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2AuditGrantedClientsClientIdTokens(var_client_id, next_page_token = var_next_page_tokendata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2AuditGrantedClientsClientIdTokens(var_client_id, next_page_token = var_next_page_token)
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2AuditTokensTokenIdMetadata  ####################
#'
#' library(synclient)
#' var_token_id <- "token_id_example" # character | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2AuditTokensTokenIdMetadata(var_token_iddata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2AuditTokensTokenIdMetadata(var_token_id)
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2Client  ####################
#'
#' library(synclient)
#' var_next_page_token <- "next_page_token_example" # character | returned along with a page of results, this is passed to   the server to retrieve the next page. (Optional)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2Client(next_page_token = var_next_page_tokendata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2Client(next_page_token = var_next_page_token)
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2ClientId  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | the ID of the client to retrieve
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2ClientId(var_iddata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2ClientId(var_id)
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2Jwks  ####################
#'
#' library(synclient)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2Jwks(data_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2Jwks()
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2TokenTokenIdMetadata  ####################
#'
#' library(synclient)
#' var_verified_oauth_client_id <- "verified_oauth_client_id_example" # character | 
#' var_token_id <- "token_id_example" # character | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2TokenTokenIdMetadata(var_verified_oauth_client_id, var_token_iddata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2TokenTokenIdMetadata(var_verified_oauth_client_id, var_token_id)
#' dput(result)
#'
#'
#' ####################  GetAuthV1Oauth2Userinfo  ####################
#'
#' library(synclient)
#' var_synapse_authorization <- "synapse_authorization_example" # character |  (Optional)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1Oauth2Userinfo(synapse_authorization = var_synapse_authorizationdata_file = "result.txt")
#' result <- api_instance$GetAuthV1Oauth2Userinfo(synapse_authorization = var_synapse_authorization)
#' dput(result)
#'
#'
#' ####################  GetAuthV1WellKnownOpenidConfiguration  ####################
#'
#' library(synclient)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetAuthV1WellKnownOpenidConfiguration(data_file = "result.txt")
#' result <- api_instance$GetAuthV1WellKnownOpenidConfiguration()
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2AuditGrantedClientsClientIdRevoke  ####################
#'
#' library(synclient)
#' var_client_id <- "client_id_example" # character | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PostAuthV1Oauth2AuditGrantedClientsClientIdRevoke(var_client_id)
#'
#'
#' ####################  PostAuthV1Oauth2AuditTokensTokenIdRevoke  ####################
#'
#' library(synclient)
#' var_token_id <- "token_id_example" # character | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PostAuthV1Oauth2AuditTokensTokenIdRevoke(var_token_id)
#'
#'
#' ####################  PostAuthV1Oauth2Client  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_oauth_o_auth_client <- org.sagebionetworks.repo.model.oauth.OAuthClient$new("client_id_example", "client_name_example", c("redirect_uris_example"), "verified_example", "client_uri_example", "policy_uri_example", "tos_uri_example", "sector_identifier_uri_example", "sector_identifier_example", "userinfo_signed_response_alg_example", "createdOn_example", "modifiedOn_example", "createdBy_example", "etag_example") # OrgSagebionetworksRepoModelOauthOAuthClient | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2Client(var_org_sagebionetworks_repo_model_oauth_o_auth_clientdata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2Client(var_org_sagebionetworks_repo_model_oauth_o_auth_client)
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2ClientSecretId  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2ClientSecretId(var_iddata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2ClientSecretId(var_id)
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2Consent  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_oauth_oidc_authorization_request <- org.sagebionetworks.repo.model.oauth.OIDCAuthorizationRequest$new("clientId_example", "scope_example", org.sagebionetworks.repo.model.oauth.OIDCClaimsRequest$new(c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example"))), c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example")))), "responseType_example", "redirectUri_example", "nonce_example", "userId_example", "authenticatedAt_example", "authorizedAt_example") # OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequest | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2Consent(var_org_sagebionetworks_repo_model_oauth_oidc_authorization_requestdata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2Consent(var_org_sagebionetworks_repo_model_oauth_oidc_authorization_request)
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2Consentcheck  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_oauth_oidc_authorization_request <- org.sagebionetworks.repo.model.oauth.OIDCAuthorizationRequest$new("clientId_example", "scope_example", org.sagebionetworks.repo.model.oauth.OIDCClaimsRequest$new(c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example"))), c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example")))), "responseType_example", "redirectUri_example", "nonce_example", "userId_example", "authenticatedAt_example", "authorizedAt_example") # OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequest | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2Consentcheck(var_org_sagebionetworks_repo_model_oauth_oidc_authorization_requestdata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2Consentcheck(var_org_sagebionetworks_repo_model_oauth_oidc_authorization_request)
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2Description  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_oauth_oidc_authorization_request <- org.sagebionetworks.repo.model.oauth.OIDCAuthorizationRequest$new("clientId_example", "scope_example", org.sagebionetworks.repo.model.oauth.OIDCClaimsRequest$new(c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example"))), c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example")))), "responseType_example", "redirectUri_example", "nonce_example", "userId_example", "authenticatedAt_example", "authorizedAt_example") # OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequest | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2Description(var_org_sagebionetworks_repo_model_oauth_oidc_authorization_requestdata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2Description(var_org_sagebionetworks_repo_model_oauth_oidc_authorization_request)
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2Revoke  ####################
#'
#' library(synclient)
#' var_verified_oauth_client_id <- "verified_oauth_client_id_example" # character | 
#' var_org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request <- org.sagebionetworks.repo.model.oauth.OAuthTokenRevocationRequest$new("token_example", "token_type_hint_example") # OrgSagebionetworksRepoModelOauthOAuthTokenRevocationRequest | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' api_instance$PostAuthV1Oauth2Revoke(var_verified_oauth_client_id, var_org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request)
#'
#'
#' ####################  PostAuthV1Oauth2Token  ####################
#'
#' library(synclient)
#' var_verified_oauth_client_id <- "verified_oauth_client_id_example" # character | 
#' var_grant_type <- org.sagebionetworks.repo.model.oauth.OAuthGrantType$new() # OrgSagebionetworksRepoModelOauthOAuthGrantType | authorization_code or refresh_token
#' var_code <- "code_example" # character | required if grant_type is authorization_code (Optional)
#' var_redirect_uri <- "redirect_uri_example" # character |  (Optional)
#' var_refresh_token <- "refresh_token_example" # character | required if grant_type is refresh_token (Optional)
#' var_scope <- "scope_example" # character | only provided if grant_type is refresh_token (Optional)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2Token(var_verified_oauth_client_id, var_grant_type, code = var_code, redirect_uri = var_redirect_uri, refresh_token = var_refresh_token, scope = var_scopedata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2Token(var_verified_oauth_client_id, var_grant_type, code = var_code, redirect_uri = var_redirect_uri, refresh_token = var_refresh_token, scope = var_scope)
#' dput(result)
#'
#'
#' ####################  PostAuthV1Oauth2Userinfo  ####################
#'
#' library(synclient)
#' var_synapse_authorization <- "synapse_authorization_example" # character |  (Optional)
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostAuthV1Oauth2Userinfo(synapse_authorization = var_synapse_authorizationdata_file = "result.txt")
#' result <- api_instance$PostAuthV1Oauth2Userinfo(synapse_authorization = var_synapse_authorization)
#' dput(result)
#'
#'
#' ####################  PutAuthV1Oauth2AuditTokensTokenIdMetadata  ####################
#'
#' library(synclient)
#' var_token_id <- "token_id_example" # character | 
#' var_org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information <- org.sagebionetworks.repo.model.oauth.OAuthRefreshTokenInformation$new("tokenId_example", "clientId_example", "principalId_example", "name_example", c("scopes_example"), org.sagebionetworks.repo.model.oauth.OIDCClaimsRequest$new(c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example"))), c(key = org.sagebionetworks.repo.model.oauth.OIDCClaimsRequestDetails$new("essential_example", "value_example", c("values_example")))), "authorizedOn_example", "lastUsed_example", "modifiedOn_example", "etag_example") # OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutAuthV1Oauth2AuditTokensTokenIdMetadata(var_token_id, var_org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_informationdata_file = "result.txt")
#' result <- api_instance$PutAuthV1Oauth2AuditTokensTokenIdMetadata(var_token_id, var_org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information)
#' dput(result)
#'
#'
#' ####################  PutAuthV1Oauth2ClientId  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | 
#' var_org_sagebionetworks_repo_model_oauth_o_auth_client <- org.sagebionetworks.repo.model.oauth.OAuthClient$new("client_id_example", "client_name_example", c("redirect_uris_example"), "verified_example", "client_uri_example", "policy_uri_example", "tos_uri_example", "sector_identifier_uri_example", "sector_identifier_example", "userinfo_signed_response_alg_example", "createdOn_example", "modifiedOn_example", "createdBy_example", "etag_example") # OrgSagebionetworksRepoModelOauthOAuthClient | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutAuthV1Oauth2ClientId(var_id, var_org_sagebionetworks_repo_model_oauth_o_auth_clientdata_file = "result.txt")
#' result <- api_instance$PutAuthV1Oauth2ClientId(var_id, var_org_sagebionetworks_repo_model_oauth_o_auth_client)
#' dput(result)
#'
#'
#' ####################  PutAuthV1Oauth2ClientIdVerificationPrecheck  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | 
#' var_org_sagebionetworks_repo_model_oauth_o_auth_client <- org.sagebionetworks.repo.model.oauth.OAuthClient$new("client_id_example", "client_name_example", c("redirect_uris_example"), "verified_example", "client_uri_example", "policy_uri_example", "tos_uri_example", "sector_identifier_uri_example", "sector_identifier_example", "userinfo_signed_response_alg_example", "createdOn_example", "modifiedOn_example", "createdBy_example", "etag_example") # OrgSagebionetworksRepoModelOauthOAuthClient | 
#'
#' api_instance <- OpenIDConnectServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutAuthV1Oauth2ClientIdVerificationPrecheck(var_id, var_org_sagebionetworks_repo_model_oauth_o_auth_clientdata_file = "result.txt")
#' result <- api_instance$PutAuthV1Oauth2ClientIdVerificationPrecheck(var_id, var_org_sagebionetworks_repo_model_oauth_o_auth_client)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
OpenIDConnectServicesApi <- R6::R6Class(
  "OpenIDConnectServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new OpenIDConnectServicesApi.
    #'
    #' @description
    #' Initialize a new OpenIDConnectServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the client to delete
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteAuthV1Oauth2ClientId = function(id, ...) {
      local_var_response <- self$DeleteAuthV1Oauth2ClientIdWithHttpInfo(id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the client to delete
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteAuthV1Oauth2ClientIdWithHttpInfo = function(id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/auth/v1/oauth2/client/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param next_page_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClientAuthorizationHistoryList
    #' @export
    GetAuthV1Oauth2AuditGrantedClients = function(next_page_token = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2AuditGrantedClientsWithHttpInfo(next_page_token, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param next_page_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClientAuthorizationHistoryList) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2AuditGrantedClientsWithHttpInfo = function(next_page_token = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["nextPageToken"]] <- `next_page_token`

      local_var_url_path <- "/auth/v1/oauth2/audit/grantedClients"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClientAuthorizationHistoryList", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param client_id 
    #' @param next_page_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformationList
    #' @export
    GetAuthV1Oauth2AuditGrantedClientsClientIdTokens = function(client_id, next_page_token = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2AuditGrantedClientsClientIdTokensWithHttpInfo(client_id, next_page_token, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param client_id 
    #' @param next_page_token (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformationList) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2AuditGrantedClientsClientIdTokensWithHttpInfo = function(client_id, next_page_token = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`client_id`)) {
        stop("Missing required parameter `client_id`.")
      }



      query_params[["nextPageToken"]] <- `next_page_token`

      local_var_url_path <- "/auth/v1/oauth2/audit/grantedClients/{clientId}/tokens"
      if (!missing(`client_id`)) {
        local_var_url_path <- gsub("\\{clientId\\}", URLencode(as.character(`client_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformationList", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param token_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation
    #' @export
    GetAuthV1Oauth2AuditTokensTokenIdMetadata = function(token_id, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2AuditTokensTokenIdMetadataWithHttpInfo(token_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param token_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2AuditTokensTokenIdMetadataWithHttpInfo = function(token_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token_id`)) {
        stop("Missing required parameter `token_id`.")
      }


      local_var_url_path <- "/auth/v1/oauth2/audit/tokens/{tokenId}/metadata"
      if (!missing(`token_id`)) {
        local_var_url_path <- gsub("\\{tokenId\\}", URLencode(as.character(`token_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param next_page_token (optional) returned along with a page of results, this is passed to   the server to retrieve the next page.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClientList
    #' @export
    GetAuthV1Oauth2Client = function(next_page_token = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2ClientWithHttpInfo(next_page_token, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param next_page_token (optional) returned along with a page of results, this is passed to   the server to retrieve the next page.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClientList) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2ClientWithHttpInfo = function(next_page_token = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      query_params[["nextPageToken"]] <- `next_page_token`

      local_var_url_path <- "/auth/v1/oauth2/client"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClientList", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the client to retrieve
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClient
    #' @export
    GetAuthV1Oauth2ClientId = function(id, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2ClientIdWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id the ID of the client to retrieve
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClient) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2ClientIdWithHttpInfo = function(id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/auth/v1/oauth2/client/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClient", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthJsonWebKeySet
    #' @export
    GetAuthV1Oauth2Jwks = function(data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2JwksWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthJsonWebKeySet) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2JwksWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/auth/v1/oauth2/jwks"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthJsonWebKeySet", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_oauth_client_id 
    #' @param token_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation
    #' @export
    GetAuthV1Oauth2TokenTokenIdMetadata = function(verified_oauth_client_id, token_id, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2TokenTokenIdMetadataWithHttpInfo(verified_oauth_client_id, token_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_oauth_client_id 
    #' @param token_id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2TokenTokenIdMetadataWithHttpInfo = function(verified_oauth_client_id, token_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`verified_oauth_client_id`)) {
        stop("Missing required parameter `verified_oauth_client_id`.")
      }

      if (missing(`token_id`)) {
        stop("Missing required parameter `token_id`.")
      }



      header_params["verifiedOauthClientId"] <- `verified_oauth_client_id`

      local_var_url_path <- "/auth/v1/oauth2/token/{tokenId}/metadata"
      if (!missing(`token_id`)) {
        local_var_url_path <- gsub("\\{tokenId\\}", URLencode(as.character(`token_id`), reserved = TRUE), local_var_url_path)
      }


      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param synapse_authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return object
    #' @export
    GetAuthV1Oauth2Userinfo = function(synapse_authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1Oauth2UserinfoWithHttpInfo(synapse_authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param synapse_authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (object) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1Oauth2UserinfoWithHttpInfo = function(synapse_authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      header_params["Synapse-Authorization"] <- `synapse_authorization`

      local_var_url_path <- "/auth/v1/oauth2/userinfo"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "object", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOIDConnectConfiguration
    #' @export
    GetAuthV1WellKnownOpenidConfiguration = function(data_file = NULL, ...) {
      local_var_response <- self$GetAuthV1WellKnownOpenidConfigurationWithHttpInfo(data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOIDConnectConfiguration) with additional information such as HTTP status code, headers
    #' @export
    GetAuthV1WellKnownOpenidConfigurationWithHttpInfo = function(data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      local_var_url_path <- "/auth/v1/.well-known/openid-configuration"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOIDConnectConfiguration", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param client_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostAuthV1Oauth2AuditGrantedClientsClientIdRevoke = function(client_id, ...) {
      local_var_response <- self$PostAuthV1Oauth2AuditGrantedClientsClientIdRevokeWithHttpInfo(client_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param client_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2AuditGrantedClientsClientIdRevokeWithHttpInfo = function(client_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`client_id`)) {
        stop("Missing required parameter `client_id`.")
      }


      local_var_url_path <- "/auth/v1/oauth2/audit/grantedClients/{clientId}/revoke"
      if (!missing(`client_id`)) {
        local_var_url_path <- gsub("\\{clientId\\}", URLencode(as.character(`client_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param token_id 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostAuthV1Oauth2AuditTokensTokenIdRevoke = function(token_id, ...) {
      local_var_response <- self$PostAuthV1Oauth2AuditTokensTokenIdRevokeWithHttpInfo(token_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param token_id 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2AuditTokensTokenIdRevokeWithHttpInfo = function(token_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token_id`)) {
        stop("Missing required parameter `token_id`.")
      }


      local_var_url_path <- "/auth/v1/oauth2/audit/tokens/{tokenId}/revoke"
      if (!missing(`token_id`)) {
        local_var_url_path <- gsub("\\{tokenId\\}", URLencode(as.character(`token_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_client 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClient
    #' @export
    PostAuthV1Oauth2Client = function(org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2ClientWithHttpInfo(org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_client 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClient) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2ClientWithHttpInfo = function(org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_oauth_o_auth_client`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_o_auth_client`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_oauth_o_auth_client`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_o_auth_client`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/client"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClient", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClientIdAndSecret
    #' @export
    PostAuthV1Oauth2ClientSecretId = function(id, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2ClientSecretIdWithHttpInfo(id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClientIdAndSecret) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2ClientSecretIdWithHttpInfo = function(id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }


      local_var_url_path <- "/auth/v1/oauth2/client/secret/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClientIdAndSecret", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_oidc_authorization_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthAuthorizationResponse
    #' @export
    PostAuthV1Oauth2Consent = function(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2ConsentWithHttpInfo(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_oidc_authorization_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthAuthorizationResponse) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2ConsentWithHttpInfo = function(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_oauth_oidc_authorization_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_oidc_authorization_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_oauth_oidc_authorization_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_oidc_authorization_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/consent"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthAuthorizationResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_oidc_authorization_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthConsentGrantedResponse
    #' @export
    PostAuthV1Oauth2Consentcheck = function(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2ConsentcheckWithHttpInfo(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_oidc_authorization_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthConsentGrantedResponse) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2ConsentcheckWithHttpInfo = function(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_oauth_oidc_authorization_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_oidc_authorization_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_oauth_oidc_authorization_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_oidc_authorization_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/consentcheck"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthConsentGrantedResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_oidc_authorization_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequestDescription
    #' @export
    PostAuthV1Oauth2Description = function(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2DescriptionWithHttpInfo(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_oauth_oidc_authorization_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequestDescription) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2DescriptionWithHttpInfo = function(org_sagebionetworks_repo_model_oauth_oidc_authorization_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_oauth_oidc_authorization_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_oidc_authorization_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_oauth_oidc_authorization_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_oidc_authorization_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/description"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOIDCAuthorizationRequestDescription", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_oauth_client_id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostAuthV1Oauth2Revoke = function(verified_oauth_client_id, org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request, ...) {
      local_var_response <- self$PostAuthV1Oauth2RevokeWithHttpInfo(verified_oauth_client_id, org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_oauth_client_id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2RevokeWithHttpInfo = function(verified_oauth_client_id, org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`verified_oauth_client_id`)) {
        stop("Missing required parameter `verified_oauth_client_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request`.")
      }



      header_params["verifiedOauthClientId"] <- `verified_oauth_client_id`

      if (!is.null(`org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_o_auth_token_revocation_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/revoke"

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_oauth_client_id 
    #' @param grant_type authorization_code or refresh_token
    #' @param code (optional) required if grant_type is authorization_code
    #' @param redirect_uri (optional) No description
    #' @param refresh_token (optional) required if grant_type is refresh_token
    #' @param scope (optional) only provided if grant_type is refresh_token
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOIDCTokenResponse
    #' @export
    PostAuthV1Oauth2Token = function(verified_oauth_client_id, grant_type, code = NULL, redirect_uri = NULL, refresh_token = NULL, scope = NULL, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2TokenWithHttpInfo(verified_oauth_client_id, grant_type, code, redirect_uri, refresh_token, scope, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param verified_oauth_client_id 
    #' @param grant_type authorization_code or refresh_token
    #' @param code (optional) required if grant_type is authorization_code
    #' @param redirect_uri (optional) No description
    #' @param refresh_token (optional) required if grant_type is refresh_token
    #' @param scope (optional) only provided if grant_type is refresh_token
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOIDCTokenResponse) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2TokenWithHttpInfo = function(verified_oauth_client_id, grant_type, code = NULL, redirect_uri = NULL, refresh_token = NULL, scope = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`verified_oauth_client_id`)) {
        stop("Missing required parameter `verified_oauth_client_id`.")
      }

      if (missing(`grant_type`)) {
        stop("Missing required parameter `grant_type`.")
      }







      header_params["verifiedOauthClientId"] <- `verified_oauth_client_id`

      query_params[["grant_type"]] <- `grant_type`

      query_params[["code"]] <- `code`

      query_params[["redirect_uri"]] <- `redirect_uri`

      query_params[["refresh_token"]] <- `refresh_token`

      query_params[["scope"]] <- `scope`

      local_var_url_path <- "/auth/v1/oauth2/token"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOIDCTokenResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param synapse_authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return object
    #' @export
    PostAuthV1Oauth2Userinfo = function(synapse_authorization = NULL, data_file = NULL, ...) {
      local_var_response <- self$PostAuthV1Oauth2UserinfoWithHttpInfo(synapse_authorization, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param synapse_authorization (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (object) with additional information such as HTTP status code, headers
    #' @export
    PostAuthV1Oauth2UserinfoWithHttpInfo = function(synapse_authorization = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE


      header_params["Synapse-Authorization"] <- `synapse_authorization`

      local_var_url_path <- "/auth/v1/oauth2/userinfo"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "object", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param token_id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation
    #' @export
    PutAuthV1Oauth2AuditTokensTokenIdMetadata = function(token_id, org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information, data_file = NULL, ...) {
      local_var_response <- self$PutAuthV1Oauth2AuditTokensTokenIdMetadataWithHttpInfo(token_id, org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param token_id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation) with additional information such as HTTP status code, headers
    #' @export
    PutAuthV1Oauth2AuditTokensTokenIdMetadataWithHttpInfo = function(token_id, org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`token_id`)) {
        stop("Missing required parameter `token_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_o_auth_refresh_token_information`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/audit/tokens/{tokenId}/metadata"
      if (!missing(`token_id`)) {
        local_var_url_path <- gsub("\\{tokenId\\}", URLencode(as.character(`token_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthRefreshTokenInformation", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_client 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClient
    #' @export
    PutAuthV1Oauth2ClientId = function(id, org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = NULL, ...) {
      local_var_response <- self$PutAuthV1Oauth2ClientIdWithHttpInfo(id, org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_client 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClient) with additional information such as HTTP status code, headers
    #' @export
    PutAuthV1Oauth2ClientIdWithHttpInfo = function(id, org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_oauth_o_auth_client`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_o_auth_client`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_oauth_o_auth_client`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_o_auth_client`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/client/{id}"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClient", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_client 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelOauthOAuthClientVerificationPrecheckResult
    #' @export
    PutAuthV1Oauth2ClientIdVerificationPrecheck = function(id, org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = NULL, ...) {
      local_var_response <- self$PutAuthV1Oauth2ClientIdVerificationPrecheckWithHttpInfo(id, org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param org_sagebionetworks_repo_model_oauth_o_auth_client 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelOauthOAuthClientVerificationPrecheckResult) with additional information such as HTTP status code, headers
    #' @export
    PutAuthV1Oauth2ClientIdVerificationPrecheckWithHttpInfo = function(id, org_sagebionetworks_repo_model_oauth_o_auth_client, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_oauth_o_auth_client`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_oauth_o_auth_client`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_oauth_o_auth_client`)) {
        local_var_body <- `org_sagebionetworks_repo_model_oauth_o_auth_client`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/auth/v1/oauth2/client/{id}/verificationPrecheck"
      if (!missing(`id`)) {
        local_var_url_path <- gsub("\\{id\\}", URLencode(as.character(`id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelOauthOAuthClientVerificationPrecheckResult", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
