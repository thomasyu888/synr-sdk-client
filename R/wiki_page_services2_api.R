#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title WikiPageServices2 operations
#' @description WikiPageServices2Api
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ DeleteRepoV1AccessRequirementOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteRepoV1EntityOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ DeleteRepoV1EvaluationOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachment } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandles } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelFileFileHandleResults} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelFileFileHandleResults
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreview } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdown } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistory } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{PaginatedResultsOfV2WikiHistorySnapshot} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfV2WikiHistorySnapshot
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1AccessRequirementOwnerIdWikiheadertree2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfV2WikiHeader} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfV2WikiHeader
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2WikiIdAttachment } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandles } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelFileFileHandleResults} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelFileFileHandleResults
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreview } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2WikiIdMarkdown } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2WikiIdWikihistory } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{PaginatedResultsOfV2WikiHistorySnapshot} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfV2WikiHistorySnapshot
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWiki2orderhint } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EntityOwnerIdWikiheadertree2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfV2WikiHeader} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfV2WikiHeader
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachment } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandles } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelFileFileHandleResults} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelFileFileHandleResults
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreview } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } file_name character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdown } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } redirect character
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistory } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } owner_id character
#' \item \emph{ @returnType } \link{PaginatedResultsOfV2WikiHistorySnapshot} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfV2WikiHistorySnapshot
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1EvaluationOwnerIdWikiheadertree2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } offset numeric
#' \item \emph{ @param } limit numeric
#' \item \emph{ @returnType } \link{PaginatedResultsOfV2WikiHeader} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : PaginatedResultsOfV2WikiHeader
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1AccessRequirementOwnerIdWiki2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1EntityOwnerIdWiki2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1EvaluationOwnerIdWiki2 } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 201 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1AccessRequirementOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersion } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1EntityOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersion } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1EntityOwnerIdWiki2orderhint } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint \link{OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1EvaluationOwnerIdWiki2WikiId } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @param } org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersion } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } wiki_version numeric
#' \item \emph{ @param } owner_id character
#' \item \emph{ @param } wiki_id character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelV2WikiV2WikiPage} \cr
#'
#'
#' \item status code : 200 | Auto-generated description
#'
#' \item return type : OrgSagebionetworksRepoModelV2WikiV2WikiPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  DeleteRepoV1AccessRequirementOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to delete.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeleteRepoV1AccessRequirementOwnerIdWiki2WikiId(var_owner_id, var_wiki_id)
#'
#'
#' ####################  DeleteRepoV1EntityOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to delete.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeleteRepoV1EntityOwnerIdWiki2WikiId(var_owner_id, var_wiki_id)
#'
#'
#' ####################  DeleteRepoV1EvaluationOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluations.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to delete.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$DeleteRepoV1EvaluationOwnerIdWiki2WikiId(var_owner_id, var_wiki_id)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning AccessRequirement.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2(var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2(var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to get.
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachment  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /accessRequirement/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandles  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandles(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandles(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreview  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /accessRequirement/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdown  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | 
#' var_wiki_id <- "wiki_id_example" # character | 
#' var_redirect <- "redirect_example" # character |  (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdown(var_owner_id, var_wiki_id, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdown(var_owner_id, var_wiki_id, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistory  ####################
#'
#' library(synclient)
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistory(var_offset, var_limit, var_wiki_id, var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistory(var_offset, var_limit, var_wiki_id, var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1AccessRequirementOwnerIdWikiheadertree2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10. (Optional)
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10. (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1AccessRequirementOwnerIdWikiheadertree2(var_owner_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetRepoV1AccessRequirementOwnerIdWikiheadertree2(var_owner_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2(var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2(var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to get.
#' var_wiki_version <- 3.4 # numeric | When included returns a specific version of a wiki. (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2WikiIdAttachment  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandles  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandles(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandles(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreview  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2WikiIdMarkdown  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | 
#' var_wiki_id <- "wiki_id_example" # character | 
#' var_redirect <- "redirect_example" # character |  (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdMarkdown(var_owner_id, var_wiki_id, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdMarkdown(var_owner_id, var_wiki_id, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2WikiIdWikihistory  ####################
#'
#' library(synclient)
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10. Limit must be 50 or less.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdWikihistory(var_offset, var_limit, var_wiki_id, var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2WikiIdWikihistory(var_offset, var_limit, var_wiki_id, var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWiki2orderhint  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWiki2orderhint(var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWiki2orderhint(var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EntityOwnerIdWikiheadertree2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10. (Optional)
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10. Limit must be 50 or less. (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EntityOwnerIdWikiheadertree2(var_owner_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetRepoV1EntityOwnerIdWikiheadertree2(var_owner_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2(var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2(var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to get.
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachment  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachment(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandles  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandles(var_owner_id, var_wiki_id, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandles(var_owner_id, var_wiki_id, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreview  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage
#' var_file_name <- "file_name_example" # character | The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
#' var_redirect <- "redirect_example" # character | When set to false, the URL will be returned as text/plain             instead of redirecting. (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreview(var_owner_id, var_wiki_id, var_file_name, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdown  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | 
#' var_wiki_id <- "wiki_id_example" # character | 
#' var_redirect <- "redirect_example" # character |  (Optional)
#' var_wiki_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdown(var_owner_id, var_wiki_id, redirect = var_redirect, wiki_version = var_wiki_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdown(var_owner_id, var_wiki_id, redirect = var_redirect, wiki_version = var_wiki_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistory  ####################
#'
#' library(synclient)
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage.
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistory(var_offset, var_limit, var_wiki_id, var_owner_iddata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistory(var_offset, var_limit, var_wiki_id, var_owner_id)
#' dput(result)
#'
#'
#' ####################  GetRepoV1EvaluationOwnerIdWikiheadertree2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_offset <- 3.4 # numeric | The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10. (Optional)
#' var_limit <- 3.4 # numeric | Limits the size of the page returned. For example, a page size             of 10 require limit = 10. (Optional)
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1EvaluationOwnerIdWikiheadertree2(var_owner_id, offset = var_offset, limit = var_limitdata_file = "result.txt")
#' result <- api_instance$GetRepoV1EvaluationOwnerIdWikiheadertree2(var_owner_id, offset = var_offset, limit = var_limit)
#' dput(result)
#'
#'
#' ####################  PostRepoV1AccessRequirementOwnerIdWiki2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owner AccessRequirement.
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page <- org.sagebionetworks.repo.model.v2.wiki.V2WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdownFileHandleId_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelV2WikiV2WikiPage | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1AccessRequirementOwnerIdWiki2(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_pagedata_file = "result.txt")
#' result <- api_instance$PostRepoV1AccessRequirementOwnerIdWiki2(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page)
#' dput(result)
#'
#'
#' ####################  PostRepoV1EntityOwnerIdWiki2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owner Entity.
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page <- org.sagebionetworks.repo.model.v2.wiki.V2WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdownFileHandleId_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelV2WikiV2WikiPage | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1EntityOwnerIdWiki2(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_pagedata_file = "result.txt")
#' result <- api_instance$PostRepoV1EntityOwnerIdWiki2(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page)
#' dput(result)
#'
#'
#' ####################  PostRepoV1EvaluationOwnerIdWiki2  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owner Evaluation.
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page <- org.sagebionetworks.repo.model.v2.wiki.V2WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdownFileHandleId_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelV2WikiV2WikiPage | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1EvaluationOwnerIdWiki2(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_pagedata_file = "result.txt")
#' result <- api_instance$PostRepoV1EvaluationOwnerIdWiki2(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page)
#' dput(result)
#'
#'
#' ####################  PutRepoV1AccessRequirementOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page <- org.sagebionetworks.repo.model.v2.wiki.V2WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdownFileHandleId_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelV2WikiV2WikiPage | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1AccessRequirementOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_pagedata_file = "result.txt")
#' result <- api_instance$PutRepoV1AccessRequirementOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page)
#' dput(result)
#'
#'
#' ####################  PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersion  ####################
#'
#' library(synclient)
#' var_wiki_version <- 3.4 # numeric | 
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Access Requirement.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersion(var_wiki_version, var_owner_id, var_wiki_iddata_file = "result.txt")
#' result <- api_instance$PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersion(var_wiki_version, var_owner_id, var_wiki_id)
#' dput(result)
#'
#'
#' ####################  PutRepoV1EntityOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page <- org.sagebionetworks.repo.model.v2.wiki.V2WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdownFileHandleId_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelV2WikiV2WikiPage | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1EntityOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_pagedata_file = "result.txt")
#' result <- api_instance$PutRepoV1EntityOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page)
#' dput(result)
#'
#'
#' ####################  PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersion  ####################
#'
#' library(synclient)
#' var_wiki_version <- 3.4 # numeric | 
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersion(var_wiki_version, var_owner_id, var_wiki_iddata_file = "result.txt")
#' result <- api_instance$PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersion(var_wiki_version, var_owner_id, var_wiki_id)
#' dput(result)
#'
#'
#' ####################  PutRepoV1EntityOwnerIdWiki2orderhint  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | 
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint <- org.sagebionetworks.repo.model.v2.wiki.V2WikiOrderHint$new("ownerId_example", "ownerObjectType_example", c("idList_example"), "etag_example") # OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1EntityOwnerIdWiki2orderhint(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hintdata_file = "result.txt")
#' result <- api_instance$PutRepoV1EntityOwnerIdWiki2orderhint(var_owner_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint)
#' dput(result)
#'
#'
#' ####################  PutRepoV1EvaluationOwnerIdWiki2WikiId  ####################
#'
#' library(synclient)
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Evaluation.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#' var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page <- org.sagebionetworks.repo.model.v2.wiki.V2WikiPage$new("id_example", "etag_example", "createdOn_example", "createdBy_example", "modifiedOn_example", "modifiedBy_example", "parentWikiId_example", "title_example", "markdownFileHandleId_example", c("attachmentFileHandleIds_example")) # OrgSagebionetworksRepoModelV2WikiV2WikiPage | 
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1EvaluationOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_pagedata_file = "result.txt")
#' result <- api_instance$PutRepoV1EvaluationOwnerIdWiki2WikiId(var_owner_id, var_wiki_id, var_org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page)
#' dput(result)
#'
#'
#' ####################  PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersion  ####################
#'
#' library(synclient)
#' var_wiki_version <- 3.4 # numeric | 
#' var_owner_id <- "owner_id_example" # character | The ID of the owning Entity.
#' var_wiki_id <- "wiki_id_example" # character | The ID of the WikiPage to update.
#'
#' api_instance <- WikiPageServices2Api$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersion(var_wiki_version, var_owner_id, var_wiki_iddata_file = "result.txt")
#' result <- api_instance$PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersion(var_wiki_version, var_owner_id, var_wiki_id)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
WikiPageServices2Api <- R6::R6Class(
  "WikiPageServices2Api",
  public = list(
    api_client = NULL,
    #' Initialize a new WikiPageServices2Api.
    #'
    #' @description
    #' Initialize a new WikiPageServices2Api.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteRepoV1AccessRequirementOwnerIdWiki2WikiId = function(owner_id, wiki_id, ...) {
      local_var_response <- self$DeleteRepoV1AccessRequirementOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteRepoV1AccessRequirementOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteRepoV1EntityOwnerIdWiki2WikiId = function(owner_id, wiki_id, ...) {
      local_var_response <- self$DeleteRepoV1EntityOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteRepoV1EntityOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluations.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    DeleteRepoV1EvaluationOwnerIdWiki2WikiId = function(owner_id, wiki_id, ...) {
      local_var_response <- self$DeleteRepoV1EvaluationOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluations.
    #' @param wiki_id The ID of the WikiPage to delete.
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    DeleteRepoV1EvaluationOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }



      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "DELETE",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning AccessRequirement.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2 = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning AccessRequirement.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiId = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /accessRequirement/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachment = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentWithHttpInfo(owner_id, wiki_id, file_name, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /accessRequirement/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }






      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}/attachment"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelFileFileHandleResults
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandles = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandlesWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelFileFileHandleResults) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmenthandlesWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}/attachmenthandles"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelFileFileHandleResults", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /accessRequirement/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreview = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreviewWithHttpInfo(owner_id, wiki_id, file_name, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /accessRequirement/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdAttachmentpreviewWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }






      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}/attachmentpreview"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param wiki_id 
    #' @param redirect (optional) No description
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdown = function(owner_id, wiki_id, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdownWithHttpInfo(owner_id, wiki_id, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param wiki_id 
    #' @param redirect (optional) No description
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdMarkdownWithHttpInfo = function(owner_id, wiki_id, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }





      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}/markdown"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param wiki_id The ID of the WikiPage.
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfV2WikiHistorySnapshot
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistory = function(offset, limit, wiki_id, owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistoryWithHttpInfo(offset, limit, wiki_id, owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param wiki_id The ID of the WikiPage.
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfV2WikiHistorySnapshot) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWiki2WikiIdWikihistoryWithHttpInfo = function(offset, limit, wiki_id, owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}/wikihistory"
      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfV2WikiHistorySnapshot", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfV2WikiHeader
    #' @export
    GetRepoV1AccessRequirementOwnerIdWikiheadertree2 = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1AccessRequirementOwnerIdWikiheadertree2WithHttpInfo(owner_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfV2WikiHeader) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1AccessRequirementOwnerIdWikiheadertree2WithHttpInfo = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wikiheadertree2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfV2WikiHeader", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    GetRepoV1EntityOwnerIdWiki2 = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) When included returns a specific version of a wiki.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiId = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) When included returns a specific version of a wiki.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdAttachment = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentWithHttpInfo(owner_id, wiki_id, file_name, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }






      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}/attachment"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelFileFileHandleResults
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandles = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandlesWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelFileFileHandleResults) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdAttachmenthandlesWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}/attachmenthandles"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelFileFileHandleResults", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreview = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreviewWithHttpInfo(owner_id, wiki_id, file_name, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.entity.ownerId.wiki.wikiId.attachmenthandles}\">,GET             /entity/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>, method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdAttachmentpreviewWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }






      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}/attachmentpreview"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param wiki_id 
    #' @param redirect (optional) No description
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdMarkdown = function(owner_id, wiki_id, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WikiIdMarkdownWithHttpInfo(owner_id, wiki_id, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param wiki_id 
    #' @param redirect (optional) No description
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdMarkdownWithHttpInfo = function(owner_id, wiki_id, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }





      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}/markdown"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit Limits the size of the page returned. For example, a page size             of 10 require limit = 10. Limit must be 50 or less.
    #' @param wiki_id The ID of the WikiPage.
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfV2WikiHistorySnapshot
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdWikihistory = function(offset, limit, wiki_id, owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2WikiIdWikihistoryWithHttpInfo(offset, limit, wiki_id, owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit Limits the size of the page returned. For example, a page size             of 10 require limit = 10. Limit must be 50 or less.
    #' @param wiki_id The ID of the WikiPage.
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfV2WikiHistorySnapshot) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2WikiIdWikihistoryWithHttpInfo = function(offset, limit, wiki_id, owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}/wikihistory"
      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfV2WikiHistorySnapshot", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint
    #' @export
    GetRepoV1EntityOwnerIdWiki2orderhint = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWiki2orderhintWithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWiki2orderhintWithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2orderhint"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10. Limit must be 50 or less.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfV2WikiHeader
    #' @export
    GetRepoV1EntityOwnerIdWikiheadertree2 = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EntityOwnerIdWikiheadertree2WithHttpInfo(owner_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10. Limit must be 50 or less.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfV2WikiHeader) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EntityOwnerIdWikiheadertree2WithHttpInfo = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wikiheadertree2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfV2WikiHeader", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2 = function(owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WithHttpInfo(owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WithHttpInfo = function(owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }


      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiId = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to get.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachment = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentWithHttpInfo(owner_id, wiki_id, file_name, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }






      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}/attachment"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelFileFileHandleResults
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandles = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandlesWithHttpInfo(owner_id, wiki_id, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelFileFileHandleResults) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmenthandlesWithHttpInfo = function(owner_id, wiki_id, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}/attachmenthandles"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelFileFileHandleResults", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreview = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreviewWithHttpInfo(owner_id, wiki_id, file_name, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation
    #' @param wiki_id The ID of the WikiPage
    #' @param file_name The name of the file to get. The file names can be found in             the FileHandles from the ,<a href=\"${GET.evaluation.ownerId.wiki.wikiId.attachmenthandles}\">,GET /evaluation/{ownerId}/wiki/{wikiId}/attachmenthandles,</a>,             method.
    #' @param redirect (optional) When set to false, the URL will be returned as text/plain             instead of redirecting.
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdAttachmentpreviewWithHttpInfo = function(owner_id, wiki_id, file_name, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`file_name`)) {
        stop("Missing required parameter `file_name`.")
      }






      query_params[["fileName"]] <- `file_name`

      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}/attachmentpreview"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param wiki_id 
    #' @param redirect (optional) No description
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdown = function(owner_id, wiki_id, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdownWithHttpInfo(owner_id, wiki_id, redirect, wiki_version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param wiki_id 
    #' @param redirect (optional) No description
    #' @param wiki_version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdMarkdownWithHttpInfo = function(owner_id, wiki_id, redirect = NULL, wiki_version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }





      query_params[["redirect"]] <- `redirect`

      query_params[["wikiVersion"]] <- `wiki_version`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}/markdown"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param wiki_id The ID of the WikiPage.
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfV2WikiHistorySnapshot
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistory = function(offset, limit, wiki_id, owner_id, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistoryWithHttpInfo(offset, limit, wiki_id, owner_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param offset The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param wiki_id The ID of the WikiPage.
    #' @param owner_id The ID of the owning Entity.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfV2WikiHistorySnapshot) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWiki2WikiIdWikihistoryWithHttpInfo = function(offset, limit, wiki_id, owner_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`offset`)) {
        stop("Missing required parameter `offset`.")
      }

      if (missing(`limit`)) {
        stop("Missing required parameter `limit`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }





      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}/wikihistory"
      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfV2WikiHistorySnapshot", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return PaginatedResultsOfV2WikiHeader
    #' @export
    GetRepoV1EvaluationOwnerIdWikiheadertree2 = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1EvaluationOwnerIdWikiheadertree2WithHttpInfo(owner_id, offset, limit, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param offset (optional) The index of the pagination offset. For a page size of 10, the             first page would be at offset = 0, and the second page would             be at offset = 10.
    #' @param limit (optional) Limits the size of the page returned. For example, a page size             of 10 require limit = 10.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (PaginatedResultsOfV2WikiHeader) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1EvaluationOwnerIdWikiheadertree2WithHttpInfo = function(owner_id, offset = NULL, limit = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }




      query_params[["offset"]] <- `offset`

      query_params[["limit"]] <- `limit`

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wikiheadertree2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "PaginatedResultsOfV2WikiHeader", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner AccessRequirement.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PostRepoV1AccessRequirementOwnerIdWiki2 = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1AccessRequirementOwnerIdWiki2WithHttpInfo(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner AccessRequirement.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1AccessRequirementOwnerIdWiki2WithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Entity.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PostRepoV1EntityOwnerIdWiki2 = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1EntityOwnerIdWiki2WithHttpInfo(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Entity.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1EntityOwnerIdWiki2WithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Evaluation.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PostRepoV1EvaluationOwnerIdWiki2 = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1EvaluationOwnerIdWiki2WithHttpInfo(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owner Evaluation.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1EvaluationOwnerIdWiki2WithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PutRepoV1AccessRequirementOwnerIdWiki2WikiId = function(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`.")
      }




      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param wiki_version 
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersion = function(wiki_version, owner_id, wiki_id, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersionWithHttpInfo(wiki_version, owner_id, wiki_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param wiki_version 
    #' @param owner_id The ID of the owning Access Requirement.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1AccessRequirementOwnerIdWiki2WikiIdWikiVersionWithHttpInfo = function(wiki_version, owner_id, wiki_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`wiki_version`)) {
        stop("Missing required parameter `wiki_version`.")
      }

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      local_var_url_path <- "/repo/v1/access_requirement/{ownerId}/wiki2/{wikiId}/{wikiVersion}"
      if (!missing(`wiki_version`)) {
        local_var_url_path <- gsub("\\{wikiVersion\\}", URLencode(as.character(`wiki_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PutRepoV1EntityOwnerIdWiki2WikiId = function(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1EntityOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1EntityOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`.")
      }




      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param wiki_version 
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersion = function(wiki_version, owner_id, wiki_id, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersionWithHttpInfo(wiki_version, owner_id, wiki_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param wiki_version 
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1EntityOwnerIdWiki2WikiIdWikiVersionWithHttpInfo = function(wiki_version, owner_id, wiki_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`wiki_version`)) {
        stop("Missing required parameter `wiki_version`.")
      }

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2/{wikiId}/{wikiVersion}"
      if (!missing(`wiki_version`)) {
        local_var_url_path <- gsub("\\{wikiVersion\\}", URLencode(as.character(`wiki_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint
    #' @export
    PutRepoV1EntityOwnerIdWiki2orderhint = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1EntityOwnerIdWiki2orderhintWithHttpInfo(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id 
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1EntityOwnerIdWiki2orderhintWithHttpInfo = function(owner_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint`.")
      }



      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_order_hint`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/entity/{ownerId}/wiki2orderhint"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiOrderHint", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PutRepoV1EvaluationOwnerIdWiki2WikiId = function(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1EvaluationOwnerIdWiki2WikiIdWithHttpInfo(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param owner_id The ID of the owning Evaluation.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1EvaluationOwnerIdWiki2WikiIdWithHttpInfo = function(owner_id, wiki_id, org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }

      if (missing(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`.")
      }




      if (!is.null(`org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`)) {
        local_var_body <- `org_sagebionetworks_repo_model_v2_wiki_v2_wiki_page`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}"
      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param wiki_version 
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelV2WikiV2WikiPage
    #' @export
    PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersion = function(wiki_version, owner_id, wiki_id, data_file = NULL, ...) {
      local_var_response <- self$PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersionWithHttpInfo(wiki_version, owner_id, wiki_id, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param wiki_version 
    #' @param owner_id The ID of the owning Entity.
    #' @param wiki_id The ID of the WikiPage to update.
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelV2WikiV2WikiPage) with additional information such as HTTP status code, headers
    #' @export
    PutRepoV1EvaluationOwnerIdWiki2WikiIdWikiVersionWithHttpInfo = function(wiki_version, owner_id, wiki_id, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`wiki_version`)) {
        stop("Missing required parameter `wiki_version`.")
      }

      if (missing(`owner_id`)) {
        stop("Missing required parameter `owner_id`.")
      }

      if (missing(`wiki_id`)) {
        stop("Missing required parameter `wiki_id`.")
      }




      local_var_url_path <- "/repo/v1/evaluation/{ownerId}/wiki2/{wikiId}/{wikiVersion}"
      if (!missing(`wiki_version`)) {
        local_var_url_path <- gsub("\\{wikiVersion\\}", URLencode(as.character(`wiki_version`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`owner_id`)) {
        local_var_url_path <- gsub("\\{ownerId\\}", URLencode(as.character(`owner_id`), reserved = TRUE), local_var_url_path)
      }

      if (!missing(`wiki_id`)) {
        local_var_url_path <- gsub("\\{wikiId\\}", URLencode(as.character(`wiki_id`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "PUT",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelV2WikiV2WikiPage", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
