#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title LogService operations
#' @description LogServiceApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ PostRepoV1Log } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } user_agent character
#' \item \emph{ @param } org_sagebionetworks_repo_model_log_entry \link{OrgSagebionetworksRepoModelLogEntry}
#'
#'
#' \item status code : 200 | Void
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  PostRepoV1Log  ####################
#'
#' library(synclient)
#' var_user_agent <- "user_agent_example" # character | 
#' var_org_sagebionetworks_repo_model_log_entry <- org.sagebionetworks.repo.model.LogEntry$new("label_example", "message_example", "stacktrace_example") # OrgSagebionetworksRepoModelLogEntry | 
#'
#' api_instance <- LogServiceApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' api_instance$PostRepoV1Log(var_user_agent, var_org_sagebionetworks_repo_model_log_entry)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
LogServiceApi <- R6::R6Class(
  "LogServiceApi",
  public = list(
    api_client = NULL,
    #' Initialize a new LogServiceApi.
    #'
    #' @description
    #' Initialize a new LogServiceApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param user_agent 
    #' @param org_sagebionetworks_repo_model_log_entry 
    #' @param ... Other optional arguments
    #' @return void
    #' @export
    PostRepoV1Log = function(user_agent, org_sagebionetworks_repo_model_log_entry, ...) {
      local_var_response <- self$PostRepoV1LogWithHttpInfo(user_agent, org_sagebionetworks_repo_model_log_entry, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param user_agent 
    #' @param org_sagebionetworks_repo_model_log_entry 
    #' @param ... Other optional arguments
    #' @return API response (void) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1LogWithHttpInfo = function(user_agent, org_sagebionetworks_repo_model_log_entry, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`user_agent`)) {
        stop("Missing required parameter `user_agent`.")
      }

      if (missing(`org_sagebionetworks_repo_model_log_entry`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_log_entry`.")
      }



      header_params["User-Agent"] <- `user_agent`

      if (!is.null(`org_sagebionetworks_repo_model_log_entry`)) {
        local_var_body <- `org_sagebionetworks_repo_model_log_entry`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/log"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list()

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        local_var_resp$content <- NULL
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
