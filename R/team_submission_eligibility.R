# Platform Repository Service
#
# Platform Repository Service - Sage Bionetworks Platform 
#
# The version of the OpenAPI document: develop-SNAPSHOT
# Contact: thomas.yu@sagebionetworks.org
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title TeamSubmissionEligibility
#'
#' @description TeamSubmissionEligibility Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field eligibilityStateHash  integer [optional]
#'
#' @field evaluationId  character [optional]
#'
#' @field membersEligibility  list( \link{MemberSubmissionEligibility} ) [optional]
#'
#' @field teamEligibility  \link{SubmissionEligibility} [optional]
#'
#' @field teamId  character [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
TeamSubmissionEligibility <- R6::R6Class(
  'TeamSubmissionEligibility',
  public = list(
    `eligibilityStateHash` = NULL,
    `evaluationId` = NULL,
    `membersEligibility` = NULL,
    `teamEligibility` = NULL,
    `teamId` = NULL,
    initialize = function(
        `eligibilityStateHash`=NULL, `evaluationId`=NULL, `membersEligibility`=NULL, `teamEligibility`=NULL, `teamId`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`eligibilityStateHash`)) {
        stopifnot(is.numeric(`eligibilityStateHash`), length(`eligibilityStateHash`) == 1)
        self$`eligibilityStateHash` <- `eligibilityStateHash`
      }
      if (!is.null(`evaluationId`)) {
        stopifnot(is.character(`evaluationId`), length(`evaluationId`) == 1)
        self$`evaluationId` <- `evaluationId`
      }
      if (!is.null(`membersEligibility`)) {
        stopifnot(is.vector(`membersEligibility`), length(`membersEligibility`) != 0)
        sapply(`membersEligibility`, function(x) stopifnot(R6::is.R6(x)))
        self$`membersEligibility` <- `membersEligibility`
      }
      if (!is.null(`teamEligibility`)) {
        stopifnot(R6::is.R6(`teamEligibility`))
        self$`teamEligibility` <- `teamEligibility`
      }
      if (!is.null(`teamId`)) {
        stopifnot(is.character(`teamId`), length(`teamId`) == 1)
        self$`teamId` <- `teamId`
      }
    },
    toJSON = function() {
      TeamSubmissionEligibilityObject <- list()
      if (!is.null(self$`eligibilityStateHash`)) {
        TeamSubmissionEligibilityObject[['eligibilityStateHash']] <-
          self$`eligibilityStateHash`
      }
      if (!is.null(self$`evaluationId`)) {
        TeamSubmissionEligibilityObject[['evaluationId']] <-
          self$`evaluationId`
      }
      if (!is.null(self$`membersEligibility`)) {
        TeamSubmissionEligibilityObject[['membersEligibility']] <-
          lapply(self$`membersEligibility`, function(x) x$toJSON())
      }
      if (!is.null(self$`teamEligibility`)) {
        TeamSubmissionEligibilityObject[['teamEligibility']] <-
          self$`teamEligibility`$toJSON()
      }
      if (!is.null(self$`teamId`)) {
        TeamSubmissionEligibilityObject[['teamId']] <-
          self$`teamId`
      }

      TeamSubmissionEligibilityObject
    },
    fromJSON = function(TeamSubmissionEligibilityJson) {
      TeamSubmissionEligibilityObject <- jsonlite::fromJSON(TeamSubmissionEligibilityJson)
      if (!is.null(TeamSubmissionEligibilityObject$`eligibilityStateHash`)) {
        self$`eligibilityStateHash` <- TeamSubmissionEligibilityObject$`eligibilityStateHash`
      }
      if (!is.null(TeamSubmissionEligibilityObject$`evaluationId`)) {
        self$`evaluationId` <- TeamSubmissionEligibilityObject$`evaluationId`
      }
      if (!is.null(TeamSubmissionEligibilityObject$`membersEligibility`)) {
        self$`membersEligibility` <- ApiClient$new()$deserializeObj(TeamSubmissionEligibilityObject$`membersEligibility`, "array[MemberSubmissionEligibility]", loadNamespace("synclient"))
      }
      if (!is.null(TeamSubmissionEligibilityObject$`teamEligibility`)) {
        teamEligibilityObject <- SubmissionEligibility$new()
        teamEligibilityObject$fromJSON(jsonlite::toJSON(TeamSubmissionEligibilityObject$teamEligibility, auto_unbox = TRUE, digits = NA))
        self$`teamEligibility` <- teamEligibilityObject
      }
      if (!is.null(TeamSubmissionEligibilityObject$`teamId`)) {
        self$`teamId` <- TeamSubmissionEligibilityObject$`teamId`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`eligibilityStateHash`)) {
        sprintf(
        '"eligibilityStateHash":
          %d
                ',
        self$`eligibilityStateHash`
        )},
        if (!is.null(self$`evaluationId`)) {
        sprintf(
        '"evaluationId":
          "%s"
                ',
        self$`evaluationId`
        )},
        if (!is.null(self$`membersEligibility`)) {
        sprintf(
        '"membersEligibility":
        [%s]
',
        paste(sapply(self$`membersEligibility`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )},
        if (!is.null(self$`teamEligibility`)) {
        sprintf(
        '"teamEligibility":
        %s
        ',
        jsonlite::toJSON(self$`teamEligibility`$toJSON(), auto_unbox=TRUE, digits = NA)
        )},
        if (!is.null(self$`teamId`)) {
        sprintf(
        '"teamId":
          "%s"
                ',
        self$`teamId`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(TeamSubmissionEligibilityJson) {
      TeamSubmissionEligibilityObject <- jsonlite::fromJSON(TeamSubmissionEligibilityJson)
      self$`eligibilityStateHash` <- TeamSubmissionEligibilityObject$`eligibilityStateHash`
      self$`evaluationId` <- TeamSubmissionEligibilityObject$`evaluationId`
      self$`membersEligibility` <- ApiClient$new()$deserializeObj(TeamSubmissionEligibilityObject$`membersEligibility`, "array[MemberSubmissionEligibility]", loadNamespace("synclient"))
      self$`teamEligibility` <- SubmissionEligibility$new()$fromJSON(jsonlite::toJSON(TeamSubmissionEligibilityObject$teamEligibility, auto_unbox = TRUE, digits = NA))
      self$`teamId` <- TeamSubmissionEligibilityObject$`teamId`
      self
    }
  )
)

