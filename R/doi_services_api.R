#' Synapse REST API
#'
#' No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
#'
#' The version of the OpenAPI document: v1
#' Generated by: https://openapi-generator.tech
#'
#' @docType class
#' @title DOIServices operations
#' @description DOIServicesApi
#' @format An \code{R6Class} generator object
#' @field api_client Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ GetRepoV1Doi } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } type \link{OrgSagebionetworksRepoModelObjectType}
#' \item \emph{ @param } version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDoiV2Doi} \cr
#'
#'
#' \item status code : 200 | The DOI and all its associated DOI metadata, if the DOI has been minted for the object
#'
#' \item return type : OrgSagebionetworksRepoModelDoiV2Doi
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1DoiAssociation } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } type \link{OrgSagebionetworksRepoModelObjectType}
#' \item \emph{ @param } version numeric
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDoiV2DoiAssociation} \cr
#'
#'
#' \item status code : 200 | The DOI if the DOI has been minted for the object
#'
#' \item return type : OrgSagebionetworksRepoModelDoiV2DoiAssociation
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1DoiAsyncGetAsyncToken } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } async_token character
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelDoiV2DoiResponse} \cr
#'
#'
#' \item status code : 200 | The results of the call
#'
#' \item return type : OrgSagebionetworksRepoModelDoiV2DoiResponse
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ GetRepoV1DoiLocate } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } id character
#' \item \emph{ @param } type \link{OrgSagebionetworksRepoModelObjectType}
#' \item \emph{ @param } version numeric
#' \item \emph{ @param } redirect character
#' \item \emph{ @returnType } \link{AnyType} \cr
#'
#'
#' \item status code : 200 | Status 200 will be returned if the 'redirect' boolean param is false
#'
#' \item return type : AnyType
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' \item status code : 307 | Status 307 will be returned if the 'redirect' boolean param is true or null
#'
#'
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ PostRepoV1DoiAsyncStart } \emph{  }
#'
#' \itemize{
#' \item \emph{ @param } org_sagebionetworks_repo_model_doi_v2_doi_request \link{OrgSagebionetworksRepoModelDoiV2DoiRequest}
#' \item \emph{ @returnType } \link{OrgSagebionetworksRepoModelAsynchAsyncJobId} \cr
#'
#'
#' \item status code : 201 | The asynchronous job ID
#'
#' \item return type : OrgSagebionetworksRepoModelAsynchAsyncJobId
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  GetRepoV1Doi  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | 
#' var_type <- org.sagebionetworks.repo.model.ObjectType$new() # OrgSagebionetworksRepoModelObjectType | 
#' var_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- DOIServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1Doi(var_id, var_type, version = var_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1Doi(var_id, var_type, version = var_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1DoiAssociation  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | 
#' var_type <- org.sagebionetworks.repo.model.ObjectType$new() # OrgSagebionetworksRepoModelObjectType | 
#' var_version <- 3.4 # numeric |  (Optional)
#'
#' api_instance <- DOIServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1DoiAssociation(var_id, var_type, version = var_versiondata_file = "result.txt")
#' result <- api_instance$GetRepoV1DoiAssociation(var_id, var_type, version = var_version)
#' dput(result)
#'
#'
#' ####################  GetRepoV1DoiAsyncGetAsyncToken  ####################
#'
#' library(synclient)
#' var_async_token <- "async_token_example" # character | The async job token from the create/update call
#'
#' api_instance <- DOIServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1DoiAsyncGetAsyncToken(var_async_tokendata_file = "result.txt")
#' result <- api_instance$GetRepoV1DoiAsyncGetAsyncToken(var_async_token)
#' dput(result)
#'
#'
#' ####################  GetRepoV1DoiLocate  ####################
#'
#' library(synclient)
#' var_id <- "id_example" # character | 
#' var_type <- org.sagebionetworks.repo.model.ObjectType$new() # OrgSagebionetworksRepoModelObjectType | 
#' var_version <- 3.4 # numeric |  (Optional)
#' var_redirect <- "redirect_example" # character | Whether to return the URL or redirect to the URL (Optional)
#'
#' api_instance <- DOIServicesApi$new()
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$GetRepoV1DoiLocate(var_id, var_type, version = var_version, redirect = var_redirectdata_file = "result.txt")
#' result <- api_instance$GetRepoV1DoiLocate(var_id, var_type, version = var_version, redirect = var_redirect)
#' dput(result)
#'
#'
#' ####################  PostRepoV1DoiAsyncStart  ####################
#'
#' library(synclient)
#' var_org_sagebionetworks_repo_model_doi_v2_doi_request <- org.sagebionetworks.repo.model.doi.v2.DoiRequest$new("concreteType_example", org.sagebionetworks.repo.model.doi.v2.Doi$new(c(org.sagebionetworks.repo.model.doi.v2.DoiCreator$new("creatorName_example", c(org.sagebionetworks.repo.model.doi.v2.DoiNameIdentifier$new("identifier_example", "nameIdentifierScheme_example")))), c(org.sagebionetworks.repo.model.doi.v2.DoiTitle$new("title_example")), 123, org.sagebionetworks.repo.model.doi.v2.DoiResourceType$new("resourceTypeGeneral_example"), "associationId_example", "etag_example", "doiUri_example", "doiUrl_example", "objectId_example", "objectType_example", 123, "associatedBy_example", "associatedOn_example", "updatedBy_example", "updatedOn_example")) # OrgSagebionetworksRepoModelDoiV2DoiRequest | 
#'
#' api_instance <- DOIServicesApi$new()
#'
#' # Configure HTTP bearer authorization: bearerAuth
#' api_instance$api_client$bearer_token <- Sys.getenv("BEARER_TOKEN")
#'
#' # to save the result into a file, simply add the optional `data_file` parameter, e.g.
#' # result <- api_instance$PostRepoV1DoiAsyncStart(var_org_sagebionetworks_repo_model_doi_v2_doi_requestdata_file = "result.txt")
#' result <- api_instance$PostRepoV1DoiAsyncStart(var_org_sagebionetworks_repo_model_doi_v2_doi_request)
#' dput(result)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DOIServicesApi <- R6::R6Class(
  "DOIServicesApi",
  public = list(
    api_client = NULL,
    #' Initialize a new DOIServicesApi.
    #'
    #' @description
    #' Initialize a new DOIServicesApi.
    #'
    #' @param api_client An instance of API client.
    #' @export
    initialize = function(api_client) {
      if (!missing(api_client)) {
        self$api_client <- api_client
      } else {
        self$api_client <- ApiClient$new()
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param type 
    #' @param version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDoiV2Doi
    #' @export
    GetRepoV1Doi = function(id, type, version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1DoiWithHttpInfo(id, type, version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param type 
    #' @param version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDoiV2Doi) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1DoiWithHttpInfo = function(id, type, version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }




      query_params[["id"]] <- `id`

      query_params[["type"]] <- `type`

      query_params[["version"]] <- `version`

      local_var_url_path <- "/repo/v1/doi"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDoiV2Doi", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param type 
    #' @param version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDoiV2DoiAssociation
    #' @export
    GetRepoV1DoiAssociation = function(id, type, version = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1DoiAssociationWithHttpInfo(id, type, version, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param type 
    #' @param version (optional) No description
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDoiV2DoiAssociation) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1DoiAssociationWithHttpInfo = function(id, type, version = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }




      query_params[["id"]] <- `id`

      query_params[["type"]] <- `type`

      query_params[["version"]] <- `version`

      local_var_url_path <- "/repo/v1/doi/association"

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDoiV2DoiAssociation", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param async_token The async job token from the create/update call
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelDoiV2DoiResponse
    #' @export
    GetRepoV1DoiAsyncGetAsyncToken = function(async_token, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1DoiAsyncGetAsyncTokenWithHttpInfo(async_token, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param async_token The async job token from the create/update call
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelDoiV2DoiResponse) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1DoiAsyncGetAsyncTokenWithHttpInfo = function(async_token, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`async_token`)) {
        stop("Missing required parameter `async_token`.")
      }


      local_var_url_path <- "/repo/v1/doi/async/get/{asyncToken}"
      if (!missing(`async_token`)) {
        local_var_url_path <- gsub("\\{asyncToken\\}", URLencode(as.character(`async_token`), reserved = TRUE), local_var_url_path)
      }

      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelDoiV2DoiResponse", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param type 
    #' @param version (optional) No description
    #' @param redirect (optional) Whether to return the URL or redirect to the URL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return AnyType
    #' @export
    GetRepoV1DoiLocate = function(id, type, version = NULL, redirect = NULL, data_file = NULL, ...) {
      local_var_response <- self$GetRepoV1DoiLocateWithHttpInfo(id, type, version, redirect, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param id 
    #' @param type 
    #' @param version (optional) No description
    #' @param redirect (optional) Whether to return the URL or redirect to the URL
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (AnyType) with additional information such as HTTP status code, headers
    #' @export
    GetRepoV1DoiLocateWithHttpInfo = function(id, type, version = NULL, redirect = NULL, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`id`)) {
        stop("Missing required parameter `id`.")
      }

      if (missing(`type`)) {
        stop("Missing required parameter `type`.")
      }





      query_params[["id"]] <- `id`

      query_params[["type"]] <- `type`

      query_params[["version"]] <- `version`

      query_params[["redirect"]] <- `redirect`

      local_var_url_path <- "/repo/v1/doi/locate"

      # The Accept request HTTP header
      local_var_accepts <- list("text/plain")

      # The Content-Type representation header
      local_var_content_types <- list()

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "GET",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "AnyType", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_doi_v2_doi_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return OrgSagebionetworksRepoModelAsynchAsyncJobId
    #' @export
    PostRepoV1DoiAsyncStart = function(org_sagebionetworks_repo_model_doi_v2_doi_request, data_file = NULL, ...) {
      local_var_response <- self$PostRepoV1DoiAsyncStartWithHttpInfo(org_sagebionetworks_repo_model_doi_v2_doi_request, data_file = data_file, ...)
      if (local_var_response$status_code >= 200 && local_var_response$status_code <= 299) {
        local_var_response$content
      } else if (local_var_response$status_code >= 300 && local_var_response$status_code <= 399) {
        local_var_response
      } else if (local_var_response$status_code >= 400 && local_var_response$status_code <= 499) {
        local_var_response
      } else if (local_var_response$status_code >= 500 && local_var_response$status_code <= 599) {
        local_var_response
      }
    },
    #' 
    #'
    #' @description
    #' 
    #'
    #' @param org_sagebionetworks_repo_model_doi_v2_doi_request 
    #' @param data_file (optional) name of the data file to save the result
    #' @param ... Other optional arguments
    #' @return API response (OrgSagebionetworksRepoModelAsynchAsyncJobId) with additional information such as HTTP status code, headers
    #' @export
    PostRepoV1DoiAsyncStartWithHttpInfo = function(org_sagebionetworks_repo_model_doi_v2_doi_request, data_file = NULL, ...) {
      args <- list(...)
      query_params <- list()
      header_params <- c()
      form_params <- list()
      file_params <- list()
      local_var_body <- NULL
      oauth_scopes <- NULL
      is_oauth <- FALSE

      if (missing(`org_sagebionetworks_repo_model_doi_v2_doi_request`)) {
        stop("Missing required parameter `org_sagebionetworks_repo_model_doi_v2_doi_request`.")
      }


      if (!is.null(`org_sagebionetworks_repo_model_doi_v2_doi_request`)) {
        local_var_body <- `org_sagebionetworks_repo_model_doi_v2_doi_request`$toJSONString()
      } else {
        body <- NULL
      }

      local_var_url_path <- "/repo/v1/doi/async/start"
      # Bearer token
      if (!is.null(self$api_client$bearer_token)) {
        header_params["Authorization"] <- paste("Bearer", self$api_client$bearer_token, sep = " ")
      }

      # The Accept request HTTP header
      local_var_accepts <- list("application/json")

      # The Content-Type representation header
      local_var_content_types <- list("application/json")

      local_var_resp <- self$api_client$CallApi(url = paste0(self$api_client$base_path, local_var_url_path),
                                 method = "POST",
                                 query_params = query_params,
                                 header_params = header_params,
                                 form_params = form_params,
                                 file_params = file_params,
                                 accepts = local_var_accepts,
                                 content_types = local_var_content_types,
                                 body = local_var_body,
                                 is_oauth = is_oauth,
                                 oauth_scopes = oauth_scopes,
                                 ...)

      if (local_var_resp$status_code >= 200 && local_var_resp$status_code <= 299) {
        # save response in a file
        if (!is.null(data_file)) {
          write(local_var_resp$response, data_file)
        }

        deserialized_resp_obj <- tryCatch(
          self$api_client$deserialize(local_var_resp$response_as_text(), "OrgSagebionetworksRepoModelAsynchAsyncJobId", loadNamespace("synclient")),
          error = function(e) {
            stop("Failed to deserialize response")
          }
        )
        local_var_resp$content <- deserialized_resp_obj
        local_var_resp
      } else if (local_var_resp$status_code >= 300 && local_var_resp$status_code <= 399) {
        ApiResponse$new(paste("Server returned ", local_var_resp$status_code, " response status code."), local_var_resp)
      } else if (local_var_resp$status_code >= 400 && local_var_resp$status_code <= 499) {
        ApiResponse$new("API client error", local_var_resp)
      } else if (local_var_resp$status_code >= 500 && local_var_resp$status_code <= 599) {
        if (is.null(local_var_resp$response) || local_var_resp$response == "") {
          local_var_resp$response <- "API server error"
        }
        local_var_resp
      }
    }
  )
)
